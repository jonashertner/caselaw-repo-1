# Handoff — 2026-08-22

## MORNING 2026-08-23 — VERDICTS (read before the four checks below)

- **Embeddings: SUCCESS, 02:24 UTC** — 303,387 new vectors over 9.17M rows,
  swapped atomically. paragraph_embeddings.db is CURRENT. The
  semantic-rescue benchmark is unblocked. (Delta was 6× forecast because
  the structure rebuild renumbers paragraphs corpus-wide — recurs on every
  structure-extractor change; consider content-hash keying later.)
- **Graph shadow: RESEEDED** — the version trap fired
  (`version_mismatch:…:1`), full bootstrap 3.4h, exit 0, 1,054,206
  decisions seeded with current extraction (ATF/DTF in).
- **Structure shadow: NOT yet** — systemd killed the service at its 6h
  start-timeout (02:01:14) mid-structure-bootstrap. Self-heals Monday
  20:00 (structure still stores '2' → trap re-fires, ~4.5h run).
  TimeoutStartSec raised to 10h (drop-in deployed + in repo) so a double
  bootstrap can never hit the ceiling again. **No drift record was written
  last night** (killed before the check) — first meaningful drift verdict:
  Tuesday morning, graph pair should collapse toward green already in
  Monday night's record if the check runs after step 2.
- Sunday's nightly launched 03:30 on schedule; harvest (Step 2h) ~14:00,
  homepage-sync first lines ~09:45 (Step 6a), gate ~09:45.
- **`/search/` overhauled (`d388f388`)** after a user-uselessness report.
  Diagnosis: solid backend, wasteful page — "(untitled decision)" on every
  BGE (they carry no title; identity = citation + Regeste), court codes
  instead of names, and the API's is_leading_case / citation_count /
  snippet / pinpoint fields fetched then discarded; the example docket
  6B_1234/2025 was fabricated. Now: derived headlines (abbreviation-aware
  splitter — "Art." must not end a sentence), Leitentscheid + N×-zitiert
  badges, →E. x.y pinpoint deep links, BGE/BGer + newest-first filters
  (API supported both all along), "2'000+" lower-bound totals. Verified
  against live payloads pre-push; deploys with the docs/ push.
  Remaining search-page ideas: date-range inputs, cantonal filter
  (needs a court-code group param API-side), KWIC highlighting.
- **Capabilities panel added to /search/ (`456b10c0`), five locales,
  every claim live-verified first** — incl. NOT (99→79), sort=date_desc,
  court=bge, and the trilingual-Regeste cross-language effect
  ("responsabilité du détenteur" → BGE 131 III 115 rank 1). Ledes
  softened from "every published decision" to "1M+" (README-honest).
  The verification CAUGHT an overclaim before it shipped: the FR text
  promised ATF lookup while /api/lookup rejected ATF/DTF prefixes —
  fixed (`ee8fc623`, #43 REST slice), 6 tests, deployed + rolling
  restart + live-verified. Pattern held: verify the claim, not the hope.
- **Second sweep of the panel's operator claims found explicit OR broken
  on the decisions path** ('Mietzins OR Pachtzins' → 52, fewer than either
  operand: the classifier said operator, _sanitize_fts5 quoted it, the
  explicit branch ran the sanitised string). Fixed (`c975ea1a`) by deriving
  the explicit raw strategy from the ORIGINAL via _explicit_laws_match —
  the same #60 cure search_laws got. Live-proven: 6 of top-20 results are
  single-operand docs; collision (#88) and NOT regressions guarded; 5
  tests; suite 2081. **Residual:** explicit-OR `total` counts the fused
  pool (~110-131) not the true union (~2500) — correctly flagged
  total_is_lower_bound so the page shows "110+", but a real COUNT for the
  explicit path would be truer; same family as #75's counting work.

## MORNING 2026-08-23 — start here (fresh session, context cleared)

Four checks, then the day's work sorts itself:

```bash
ssh root@46.225.212.40 'tail -3 /var/log/opencaselaw-embeddings-topup.log'
#   → "SUCCESS — swapped": embeddings current; queue the weekly timer + the
#     semantic-rescue benchmark (see semantic-rescue-state memory).
#   → cutoff/failure: live DB untouched; rerun /root/embeddings_topup.sh (resume-safe).

ssh root@46.225.212.40 'tail -1 /opt/caselaw/repo/logs/publish_drift_check.jsonl' | python3 -m json.tool | grep -E '"pair"|"ok"|drift'
#   → green/near-green: the 08-22 reseed worked; green-week clock runs, flip ~Aug 29.
#   → still far red: the reseed missed something — first suspect the BGer↔BGE
#     cross-ref pass (48d213d4, full builder only); see shadow-cutover-state memory.

ssh root@46.225.212.40 'grep "homepage sync" /opt/caselaw/repo/logs/publish.log | tail -2'
#   → first live run of the step-6 wiring (67698d11). Expect "updated: …" or
#     "already current". Also confirm docs/index.html rides the bot's stats commit.

ssh root@46.225.212.40 'systemctl status opencaselaw-smoke --no-pager | tail -3; cat /opt/caselaw/repo/state/last_publish_success.json'
#   → smoke green; success marker from 08-23's run. Check non_fatal_failures:
#     a "5c … WARN" entry means the corpus shipped UNVERIFIED (new semantics).
```

Then the queue, in order: semantic-rescue benchmark (two commands, decides
PINPOINT_SEMANTIC_ENABLED with numbers) → dedup consumption design (serving
collapses the 140,912 mapped duplicates) → #48 date-repair pass (sized below:
11% padded, 10.3% volume-contradicting, batch mode visible) → green-week
watch. Backup remains explicitly deprioritised by Jonas (2026-08-22).

## Scholarship source expansion (#89 option 3) — probed live 2026-08-22 evening

Production corpus: 25,073 records / 24 sources, **French only 11%** (2,868).
The registry (`scrapers/scholarship/sources.py`) already scaffolds most
candidates. Live probe results:

1. **ZORA is harvestable again.** The 2026-05-27 blocker ("OAI returns 504")
   is gone: `www.zora.uzh.ch/server/oai/request` answers Identify AND
   ListIdentifiers (completeListSize **217,930**, 100/page ≈ ~40-60 min at
   the configured 1 req/s). Registry entry `zora_law` is fully configured,
   `active=False` → flip it. Expected ~9-10k law records (BORIS yielded
   7,319 from a 167k IR with the same filter). Largest law faculty in CH.
2. **UNIGE harvest is frozen at 2013 — OUR bug, not theirs.** Their OAI
   serves 2026 records fine (verified `from=2026-06-01` → 100+ ids). 867
   French records ending 2013 means the harvest died partway and never
   resumed — 13 years of Geneva law (the top French faculty) missing.
   Diagnose the resumption/cursor state in `oai_pmh.py`. Single biggest
   French win available.
3. **FOLIA = SONAR.** `folia.unifr.ch/oai2d` answers as "Swiss Open Access
   Repository" (Invenio) with global sets. So Fribourg = `sonar.rero.ch/oai2d`
   with `set=unifr` — resolves `folia_law`'s "endpoint TBD". SONAR also
   carries `usi` (Università della Svizzera italiana — would be the FIRST
   Italian academic source; corpus has ~140 it records), `hesso`, `fernuni`.
   One endpoint, several sources, existing `oai_pmh` kind — config-only.
4. **IRIS (UNIL/SERVAL) still closed:** `/server/oai/request`, `/oai/request`,
   `/server/api` all 404. Registry's "needs outreach to UNIL library" stands —
   a jh-voice email is the unblock. Lausanne = second French law faculty.
5. **`justice` (Justice-Justiz-Giustizia, trilingual judges' journal):**
   registered `kind=custom, custom_module=scrapers.scholarship.justice` but
   **the module does not exist on disk** — real scraper work, and license is
   OA-no-redistribution (metadata + abstract + link only).
6. **e-periodica language backfill:** 6,985 records (28% of corpus) have NULL
   language, so French/Italian content there is invisible to language
   filters. Data-quality fix in the e-periodica mapper.

Order of value for #89's French gap: UNIGE-fix ≥ SONAR(unifr) > ZORA (volume,
mostly de) > IRIS outreach > justice module > e-periodica backfill.

**EXECUTED same evening (`944e4bed`, deployed to VPS):** windowed OAI
harvesting (completeListSize-verified, probe-and-bisect, sub-day splitting
gated on Identify granularity, retry×3, atomic install) + registry: ZORA
activated, unige windowed, unifr_law/usi_law added via SONAR. Live-verified:
UNIGE smoke yielded 206/220 records datestamped >2013; truncation detector
fired in the wild. **Sunday 08-23's Step 2h runs the first full harvests —
expect it to run 1.5-2.5h longer than usual** (ZORA scans 218k records,
UNIGE ~124k windowed). Verify Monday: per-source counts in
`build_legal_scholarship` output — unige should jump from 1,115 toward
several thousand with post-2013 French content; zora_law appears for the
first time (~9-10k expected). Then RE-TEST #89's two repro queries against
the enriched corpus before touching ranking. Remaining: IRIS outreach
letter (jh-voice), justice module, e-periodica language backfill.

**FR/IT/RM follow-up probes (late 08-22, all live-verified):**
- **e-periodica backfill upgraded from cosmetic to material**: ZSR (=Revue
  de droit suisse = Revista da dretg svizzer) and the trilingual
  criminology cluster are ALREADY harvested (1,127 + 1,011 records via the
  ddc sets) — their French/Italian articles sit in the corpus with NULL
  language. The backfill makes existing content discoverable; no new
  scrape needed.
- **OpenAlex as a CC0 metadata layer**: CH-institution + Law-concept,
  OA-only: fr 1,006 / de 916 / it 75 works with DOIs. Includes UNIL output
  → bypasses the closed IRIS. New REST harvester (cursor pagination),
  dedupe by DOI against existing sources. Caveat: OpenAlex language/concept
  tags are noisy; treat as metadata+link records.
- **SONAR quick adds**: sets `fernuni` (UniDistance — has a law faculty,
  FR/DE) and `hesso` (droit économique) — one-line entries on the endpoint
  verified tonight.
- **Romansh is real and it is in the LAW, not scholarship**: Fedlex SPARQL
  counts **237,891 ROH-language expressions** (raw; refine to consolidated
  acts before sizing). Extend build_statutes_db to ingest RM versions —
  the schema already reserves 'rm'. Second RM+IT lever: GR lexbrowser
  publishes the Bündner Rechtsbuch in DE/IT/RM — the GR cantonal-law
  scrape could carry all three (also strengthens Italian via the Grigioni).
  Mind [[cantonal-laws-build-inputs]] before any cantonal_laws rebuild.
- **DOAJ sweep method validated** (sui generis found under country:CH);
  a systematic CH-law-journal enumeration is a 30-min follow-up.
- Outreach-class candidates (license permission needed, metadata+links
  model): LawInside.ch, cdbf.ch (FR case notes); Rivista ticinese di
  diritto (IT).

**PR #91 reviewed (request-changes posted, 2026-08-23):** external
contributor's MCP setup guides for Continue/Zed/Open WebUI, closing #71.
Zed guide verified CORRECT against Zed docs (ship as-is). Blocking:
Continue's primary block is Claude-Desktop's format (real shape:
config.yaml mcpServers list — verified against Continue docs); Open
WebUI's "Valve / MCP Connections" menu appears invented (docs still
route via mcpo proxy); both tool examples use nonexistent params
(get_law "law"→abbreviation; find_leading_cases "statute"→law_code+
article); "41 tools" should be 42. Offered to apply fixes on merge if
the contributor doesn't iterate. **Separate pre-existing bug this
surfaced: docs/mcp/index.html's own strings say "41 tools" in all five
locales — live is 42; fix independently of the PR.**

**IP/cost collection live (08-23, `9c92b9da`):** per-IP daily quotas on
MCP check_claim_support + grounded attest (shared REST buckets, throttle →
team@jonashertner.com); llm_usage gains source+client; 90-day per-IP cost
ledger `logs/llm_cost_by_ip.jsonl` (logrotate maxage 90 = the retention
promise); REST middleware stamps provenance. /datenschutz/ amended ×5
locales ("Cost and abuse control (90 days)"). Both transports live-verified
in the ledger. Tier1 (72h access.log) unchanged.

Issues closed evening of 08-22 after live verification: **#88** (article
resolution, twice-verified at scale), **#75** (small-limit lower-bound marker
now present in the reporter's exact repro), **#69** (get_doctrine 0.3s vs the
20s threshold). **#78 is 2.5/3 fixed** — OR-as-operator passes the reporter's
220=220 proof and NEAR(a b, N) works, but the DOCUMENTED `NEAR/5` form still
silently no-matches. Small queued fix: translate `NEAR/N` → `NEAR(a b, N)` in
the sanitiser AND align the schema text, then close. 19 issues remain open.


## Incident: search 500s, 12:41–13:24 UTC (self-inflicted, resolved)

The `#89` weak-match helper shipped in `4f8c1cdd` was named `_term_coverage`.
That name was already bound at line 7173 by the search reranker's scoring
function, which `_rerank_rows` calls five times. Python keeps the last
definition, so from the 12:15 rolling restart every reranked search called a
function with the wrong signature. **458 tracebacks; `search_decisions` and
`/api/decisions?query=` returned 500.** Hotfix `4456f1c6` renamed the new one
to `_scholarship_term_coverage`; verified restored.

Three things to carry forward, in order of importance:

1. **No monitor would have caught it.** `quality/smoke.py` probed health, one
   decision page, one export and publish freshness — nothing that ran a query.
   It was found because a human ran `make smoke` by hand. Fixed in `7e641ec9`:
   there is now a `search_query` probe, and `publish_freshness` is build-aware
   (it had been red ~13h of every day, so it was *already* alarming during the
   outage and would have added no signal).
2. **The suite passed 2003 tests either way**, because nothing called
   `_rerank_rows` — the hottest path in the product had zero coverage.
   `tests/test_rerank_rows_smoke.py` now executes it;
   `tests/test_no_shadowed_definitions.py` catches the collision structurally.
   Both verified against a scratch copy carrying the real bug: 13 failures with
   it, 21 passes without.
3. **Two definitions 4,800 lines apart in a 22,000-line module are invisible to
   review.** The AST guard is the only thing that scales here. If `mcp_server.py`
   is ever split, keep that guard pointed at whatever replaces it.


Session covered the GitHub issue batch #81–#90 (filed by Tomagori 2026-08-20),
plus operational findings. Written at the end of the session; verify live state
before acting.

## Do this first

**RESOLVED 2026-08-22 11:30 UTC.** The seven fixes are committed (`4f8c1cdd`)
and pushed; the VPS fast-forwarded to `9bbd2349` and its `mcp_server.py` is
tracked-clean at sha `2bd2c93b85cd36ca`, matching the committed file. No worker
restart was needed — the delta from the deployed file was one comment block.
Workers last started 09:55/10:02 UTC, before the swap window, so none booted
pre-fix code.

Original problem, for the record: production ran code that existed in no git
history, scp'd into the git tree against the `mcp-deploy-path` rule, where a
conflicted nightly `stash pop` would have reverted it silently.

Two things learned doing it, now in the `mcp-deploy-path` memory:

- `git pull --rebase` **fails on the VPS** — the tree permanently carries ~68
  dirty files and rebase demands a globally clean tree. Use
  `git merge --ff-only origin/main`, which only touches the files it updates.
- The MEMORY.md index line for `mcp-deploy-path` said the opposite of the
  memory ("deploy by scp … never git push"). That inverted one-liner is what
  loads into every session. Corrected.

Backups: pre-fix `/opt/caselaw/repo/mcp_server.py.bak-2026-08-22-pre-issue-fixes`,
as-deployed `/root/mcp_server.py.fixed-2026-08-22` (sha `8fb5893be02d859c`).

## Issue state

*Second pass, 2026-08-22 ~12:30 UTC: deployed as `a7a6ac38`, all eight workers
rolling-restarted, every claim below re-verified against live production.*

Closed, fixed, verified in production: **#81, #82, #83, #86, #87**.

Open with replies posted:
- **#85** — the reported cause (BM25 gap) was *partly* wrong: the gap gate does
  fire (gap_ratio 1.131 < 1.2 on repro A), but token coverage is the binding
  one and it stays suppressed even at a gap of 1.6. Shipped the honest
  diagnosis (response now names the gate). The absolute floor the reporter
  asked for is NOT done and needs the search benchmark. **Still the main open
  search item.**
- **#88** — **now fully fixed.** Correction to the first-pass note: the query
  did not return "nothing", it returned five articles that merely *mention*
  Art. 60 OR. Root cause was structural, not an OR problem —
  `_abbreviation_lookup_federal` rejects any query containing a space, so an
  article reference never reached it and fell through to bag-of-words BM25,
  where a provision cannot win its own query. `_article_reference_lookup_federal`
  now resolves DE/FR/IT reference forms to the named article at rank 1. This
  was internal task #50, so **#50 is done**.
- **#89** — the stopword fix stays off (measured no-op at 25k scale). Shipped
  the weak-match signal instead: term coverage on the top hit, reported and
  never used to reorder. Ranking is still wrong; it is now honest about it.

**#84** — serving-side guards shipped, corpus repair still open:
- `rule_statement` page-bleed now suppressed with an explanation
  (internal #49's cheap detector: page-number opening line, or prose in a
  language the record is not in). Screened on the `full_text` fallback only —
  a Regeste is legitimately trilingual and screening it would strip the field
  from every BGE. Same-language bleed still passes.
- `decision_date_warning` on `cite` when a BGE's stored year contradicts its
  volume (vol. N = year N + 1874, ±1). Internal #48's detector.
- `attest_response` no longer blames the draft when the *stored* date is the
  outlier — new `stored_date_suspect` problem type. Before this, dating
  BGE 76 II 346 correctly to 1950 was reported as a hallucination.
- Still open: the wrong dates and bled `full_text` in the corpus, and the
  missing Regeste. Run the volume-year rule across the whole BGE collection to
  size the defect — that is a scan, so it waits for the build window.

**Two leads for that scan, both cheap:**
1. `cite("BGE 105 Ia 349")` returns `decision_date: "1979-01-01"`. The year is
   right, but 01-01 is almost certainly a year-only date padded to day
   precision. `COUNT(*) WHERE court='bge' AND decision_date LIKE '%-01-01'`
   sizes that class in one indexed query and probably explains a large share
   of the "wrong date" reports without any volume arithmetic.
2. The volume-year rule itself (vol. N = year N + 1874, ±1) is now in
   `_bge_volume_year_mismatch` and runs on a citation string — so it can be
   applied to a `decision_id` list without touching `full_text`.

**Trap for anyone scripting against prod:** `mcp_server.STATUTES_DB_PATH`
defaults to `/root/.swiss-caselaw/statutes.db`, a stale February copy holding
**36 laws**. The workers get the real 5,528-law mirror only via
`SWISS_CASELAW_DIR=/opt/caselaw/repo/output` from their systemd unit. An
ad-hoc script on that host reads the stale DB and reports confident nonsense —
it produced three false MISSes during this session's validation before being
caught. Export `SWISS_CASELAW_DIR` explicitly.

Open, no reply, corpus-side and blocked on the build window: **#90**.

Also: **#78** improved as a side effect of the #88 work (both route through
`_has_explicit_fts_syntax`); `search_practice("Arbeitszeit OR 321")` now treats
`OR` as a literal. Not verified for the operator half, no comment posted.

Nine documentation issues (#62–#67, #70, #71, #73) have had no reply since
2026-08-10.

## Work in the tree, uncommitted

`mcp_server.py` (+~400), `search_stack/build_statutes_db.py` (+29),
`tests/test_statutes_article_num.py` (+49), and 8 new test files (71 tests).
Suite: **1975 passed, 22 skipped, 0 failed** (baseline before any edit: 1898).

`build_statutes_db.py` is NOT deployed and takes effect only on a statutes
rebuild — that rebuild also retires the two-pair alias in `get_law`.

## Internal tasks filed this session

- **#48** BGE `decision_date` vs volume-year rule (volume = year − 1874)
- **#49** `rule_statement` page-bleed from the preceding decision — the sharpest
  defect found: the citation contract holds mechanically while serving another
  case's text as quotable. Cheapest detector is the opening paragraph's language
  disagreeing with the record's `language` field.
- **#50** `search_laws` does not resolve a statute reference to the article it
  names. `search_laws("Art. 60 OR")` returns articles that *cite* Art. 60 OR.
  **DONE 2026-08-22** — see #88 above.

#48 and #49 have serving-side detectors deployed; their corpus repairs need the
build window clear and an SSH session.

**#50 is narrower than the real problem.** `search_laws("Verjährung unerlaubte
Handlung")` returns Art. 133 IPRG, Art. 142 IPRG and Art. 207 AHVV — not
Art. 60 OR, which *is* the tort limitation provision. The query expands to
`(verjährung OR prescription OR prescrizione) AND unerlaubte AND handlung` and
Art. 60 OR loses because it does not repeat the query's words. So search_laws
ranks articles that *mention* a topic over the article that *governs* it, with
no explicit reference in the query at all. The #50 fix does not touch this;
it needs concept-level ranking (headings, marginal notes, or the citation
graph as a prior). Unfiled.

## Unfiled findings worth tasks

1. **`/api/decisions/<court-prefix>` 500s** — ~77 error lines and 7 ASGI
   tracebacks per hour, identical before and after the deploy, on truncated
   paths (`bger`, `zh`, `vd`, `bge`, `ch`). Pre-existing, unreported.
2. **`search_stack/reference_extraction.py`** ordinal list stops at `sexies`, so
   "Art. 322decies StGB" extracts as **Art. 322** — a wrong edge in the citation
   graph, not a missing one. Feeds `build_reference_graph.py` (publish step 2c),
   so pipeline-gated. Sits under Paper 2's substrate.
3. **`_LEGAL_STOPWORDS` is exact-match** — `Bundesgerichts` ≠ `bundesgericht`,
   `Erwaegung` ≠ `Erwägung`. Gates two surfaces now. Two tests document it.
4. **#82 residual** — a `;` between two citations in one parenthesis can still
   end a claim span.

## Maintenance pass, 2026-08-22 afternoon (five approved items)

1. **logrotate for publish.log** — installed at `/etc/logrotate.d/opencaselaw-publish`
   (repo copy `deploy/logrotate/opencaselaw-publish`, following the ocl-tier3
   pattern). copytruncate, weekly + maxsize 200M, rotate 12. The 1.1GB
   first rotation fires at the next daily logrotate run.
2. **`swiss_caselaw.db` (0 bytes, May 21)** — deleted. Zero code references.
3. **OnlineKommentar** — verified CURRENT, not stale: served DB (1,170 rows =
   390 commentaries × ~3 languages, max date 2026-07-29) matches the live API
   exactly (total=390, newest 2026-07-29). The monthly timer works — Aug 2
   mtime is its last firing. Bumped `opencaselaw-commentaries.timer` monthly →
   **weekly** (Mon 13:00 UTC; corpus is small, resync is minutes). Deleted the
   stale 51MB March copy on the volume — same trap class as the stale
   `/root/.swiss-caselaw/statutes.db`.
4. **`_incremental` twins are LIVE, do not delete.** Written Mon–Sat 20:00 UTC
   by `opencaselaw-publish-incremental.service` → `scripts/incremental_nightly.py`
   in `--shadow` mode, validated nightly by `publish_drift_check.py`. They are
   the staging ground for replacing the 7h+ full rebuild. Open decision for
   Jonas: shadow was meant to flip to `--in-place` after one green week —
   that was 2026-05-18, three months ago. Flip or keep paying ~50GB.
5. **paragraph_embeddings top-up** — armed on the VPS: `/root/embeddings_topup.sh`
   (running, gate-waiting), log `/var/log/opencaselaw-embeddings-topup.log`.
   Waits for tonight's full publish + 20:00 incremental to exit (≥21:00 UTC),
   copies the live 18.8GB DB (immutable-held by workers — never write in
   place), encodes the ~48k-decision delta with `--restart` (the offset
   watermark is invalid across structure rebuilds; the existing-key filter
   dedups), hard-stops 03:15 UTC, atomically renames on success. Workers pick
   it up per-call, no restart. Resume = rerun the script. **Verify tomorrow:**
   `tail /var/log/opencaselaw-embeddings-topup.log` should end in SUCCESS;
   then `find_relevant_erwaegung` on any 2026 decision should show the
   semantic-rescue path active.

## Five-gap exploration (evening pass) — evidence + next moves

**Jonas's picks for the next session: (1) shadow-drift diagnosis, (2) gate
timeout≠regression fix.** Homepage sync script committed; its publish.py
wiring NOT yet applied (offered, deferred — apply it alongside the gate fix
since both edit publish.py under one review).

**1. Corpus integrity.** The dedup map ALREADY EXISTS and is fresh daily:
`representation_manifest.db` — 279,359 rows, 138,447 canonical groups,
140,912 duplicates, evidence-tagged (`byte_identical`, confidence 1.0),
GE 153k + VD 101k rows in multi-member groups (= #90's cantonal duplicates),
CH only 5.9k (mostly ch_vb — the BGE dual-id problem is NOT in the manifest;
that merge is its own work item). The missing piece is CONSUMPTION: serving
never collapses members or redirects to canonical. First shippable: search
collapses multi-member groups + `get_decision` notes the canonical id.
Ready-to-run post-window sizing for #48 (seconds, indexed):
```sql
-- padded dates:      SELECT COUNT(*) FROM decisions WHERE court='bge' AND decision_date LIKE '%-01-01';
-- volume-year sweep: SELECT decision_id, decision_date FROM decisions WHERE court='bge';
--                    (pipe through _bge_volume_year_mismatch)
```
#49's corpus repair (full_text page-bleed) still needs the language-mismatch
scan over first paragraphs — post-window, decisions.db.

**#48 SIZED (2026-08-22 18:15, post-window):** 50,481 `court='bge'` records.
Padded `-01-01` dates: **5,549 (11.0%)**. Volume-year mismatch >±1y:
**3,638 of 35,305 checkable (10.3%)**. One systematic mode visible: early
volumes (1875–1876) carrying `2020-03-15` — an ingest-batch timestamp in
`decision_date`, so a `GROUP BY decision_date HAVING COUNT(*)` huge-cluster
query will likely isolate whole repairable batches at once. Caveat: 15,176
ids (30%) didn't parse under `^bge_(?:BGE_)?(\d+)_` — the true mismatch
count is a floor; inspect those id shapes before the repair pass.

**2. Shadow flip: NOT flippable — drift is real, 12/12 nights red.**
`citation_targets` −1,521,411 (−13.2%), `decision_citations` −130,809
(−1.33%), structure pair lost 3,995 decisions' paragraphs (0.51%).
decisions/statutes pairs match. The incremental builder has per-row
input-hashing + an extractor_version gate on its base DB — but the version
is bumped MANUALLY ("if this list ever grows, bump"), so extraction-code
improvements that forgot the bump leave stale rows in the shadow forever.
Diagnosis path: (a) compare `meta.extractor_version` live vs shadow,
(b) diff one drifted decision's targets live-vs-shadow, (c) if staleness
confirmed → bump version, reseed shadow from tonight's full graph, watch
drift for a week, then flip. The −13.2% concentrated in *unresolved*
targets (resolved edges only −1.33%) suggests the full builder records
unresolved extractions the incremental path drops.

**3. QC gate: both failures were TIMEOUTS, not regressions.** 08-21:
started 11:09:20, killed at exactly 12:09:20 (3600s), no verdict file.
08-18 same (during the ad-hoc-scan incident). Today: 13 min (~781s) —
the variance is page-cache temperature, and the memory's 3,518s p90 shows
the O(corpus) scan creeping to its ceiling. Structural insight: the gate
runs AFTER the swap — it gates DISTRIBUTION (HF upload, pushes), not
serving. So a timeout cascade costs distribution while protecting nothing.
**SHIPPED 2026-08-22 evening (`67698d11`), first live run 08-23 03:30:**
  a. Timeout ≠ CRITICAL — on timeout/stall the gate returns a truthy
     "WARN (…)" outcome: no cascade, distribution proceeds, ntfy DEGRADED
     alert, recorded in last_publish_success.non_fatal_failures. Exit 1
     (a real verdict, or a crash) still returns False and still blocks.
     run_cmd gained `outcome_sink` to expose HOW a command failed.
     The 3600s ceiling deliberately NOT raised — it keeps signalling that
     the scan is outgrowing its budget.
  b. The 5c-after-Step-2 reorder was considered and DROPPED: 5c already
     sits early-tier with only light steps (5a/5b/5g) between the FTS5
     build and it, so the reorder buys minutes of cache warmth at real
     review risk. The variance driver is corpus-scale + cache pressure,
     not ordering.
  c. Medium-term unchanged: incremental gate (scan rows changed since
     last green) — worth doing regardless of the cutover.
  Homepage wiring shipped in the same commit: step_6_git_push runs
  scripts/sync_homepage_fallbacks.py behind a cannot-fail helper and
  pushes docs/index.html at both push points. The proposal doc is done.

**4. Semantic rescue: bench exists and is purpose-built.**
`benchmarks/pinpoint_regeste_bench.py` — Regeste E-hints as ground truth,
reports hit/no-match "by source (lexical vs semantic)", designed to run on
the VPS. Eval plan (next session, after tonight's top-up SUCCESS):
```
PINPOINT_SEMANTIC_ENABLED=false python3 -m benchmarks.pinpoint_regeste_bench --limit 500 --court bge
PINPOINT_SEMANTIC_ENABLED=true  python3 -m benchmarks.pinpoint_regeste_bench --limit 500 --court bge
```
Compare hit/no-match; #85's repro A/B are the qualitative check. RAM for
enabling in workers: model ~500MB × 8 ≈ +4GB (host has ~53GB free) plus
first-call latency per worker; warmup call in the unit would mask it.

**5. Homepage regeneration: BUILT + tested, wiring pending approval.**
`scripts/sync_homepage_fallbacks.py` (+7 tests) rewrites the six hydration
ids from stats.json; strict (missing id / implausible value → exit 1,
nothing written); ran on the real homepage: 991'298→1'054'206, 109→118,
21'108→21'136, 8.9M→9.8M, delta 198→171, date 2026-06-21→2026-08-22.
The site serves from docs/ via the git push, so committing docs/index.html
IS the deploy. publish.py wiring (one call + one path, gated): in
step_6_git_push, run the script non-fatally before the diff check and add
"docs/index.html" to `paths`. Both push points (6a early, 6 final) then
carry it.

## Build / ops notes

- The push failure that looked like a permanent non-fast-forward divergence
  **self-heals**: `publish.py` stashes, `pull --rebase`, pops, pushes at
  finalization. It stopped pushing 08-20→08-22 because runs were not reaching
  that step, not because the divergence was unrecoverable.
- `opencaselaw-smoke.service` was failing on `publish_freshness`
  ("last successful publish 38h ago") — should clear now that 08-22 pushed.
- Corpus growth is flat: median ~123 decisions/day, unchanged since April.
- `duplicate_representations` frozen at 140,912 (13.4% of corpus) — one new
  duplicate in a month while the corpus grew +3,492. It never self-heals.
- Identity/normalisation is one programme across #76, #79, #90 and internal
  #40, #48, #49.

## Technique worth reusing

`docs/stats.json` has 420 commits back to 2026-02-12 and `docs/quality.json` 94
days. `git log --follow` + `git show <sha>:<path>` gives months of corpus and
QC trends with no VPS access and no corpus scan — the cheapest instrumentation
in the project.
