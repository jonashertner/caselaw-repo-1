# Handoff — 2026-08-22 (+ 08-23/24/25 sessions)

## 2026-08-25 05:00 UTC — READ THIS FIRST

Session ended with Bash blocked by a safety classifier (local AND VPS), so
the items marked TODO below are written but NOT executed. Everything else
here is deployed and verified.

### ★ THE CUTOVER GATE: reference_graph is GREEN. structure is not.

First post-reseed drift verdict (run 2026-08-25T00:44, after the reseed
finished 01:45 in 5h44m, ok=True):

    reference_graph      ok=TRUE   <-- was 13.20% drift, now 0.260%
      citation_targets   live 11,530,929  shadow 11,500,921  -30,008  (0.260%)
      decision_citations live  9,838,396  shadow  9,838,429      +33  (0.000%)
      decision_statutes  live 12,467,543  shadow 12,467,558      +15  (0.000%)
      decisions          live  1,054,242  shadow  1,054,592     +350  (0.033%)
      statutes                                                     0  (0.000%)
    decision_structure   ok=FALSE
      erwaegungen_paragraph: coverage LOST 3,995 current decisions (0.51%)
      exceeds 0.05% (gained 1,313)

**The extractor_version diagnosis was right** — the graph collapsed from
13.2% to 0.26% after the reseed. That half of the cutover is unblocked.

**The structure number is the tell: it lost EXACTLY 3,995 decisions both
BEFORE and AFTER the reseed (08-21 said 3,995 too).** Identical. So those
decisions are not stale — the incremental structure extractor **never
produces them at all**. A functional gap in one code path, now precisely
scoped. NEXT STEP (needs a scan, so run AFTER ~17:00 UTC — weekday load is
what starves the QC gate, see [[qc-gate-headroom]]): diff
decision_structure.db against decision_structure_incremental.db, list the
3,995 missing decision_ids, and look for what they share (court? language?
size? a parser branch?).

**PARTIAL CUTOVER IS AVAILABLE NOW:** the graph pair is green on its own,
so the graph builder could flip to `--in-place` on weekdays while the full
build still rebuilds structure — capturing the 2h24m graph-rebuild saving
without waiting for the structure fix. Jonas's call (invariant #5).

### TODO 1 — scraper coverage gaps (first alert ever to reach a human)

The 03:00 UTC ntfy alert fired for the first time because the topic was
unified on 08-24; before that these pushed to a topic with no subscriber,
which is why every gap reads "seit 11 Tagen". Six gaps, ~2,719 decisions:

    be_verwaltungsgericht  2,159  (portal 11,594 / ours 9,435)  = GitHub #68, 79% of the backlog
    vs_gerichte              399  (4,995 / 4,596)
    gr_gerichte               50  (14,856 / 14,806)
    ne_gerichte               45  (7,701 / 7,656)          <-- needs SOCKS tunnel
    ne_jurisprudence_adm      37  (1,648 / 1,611)          <-- needs SOCKS tunnel
    ju_gerichte               29  (1,170 / 1,141)          <-- needs SOCKS tunnel

Also `WARN bger: 46 discovery-phase connection failures` — bger routes
through the same tunnel (`BGER_PROXY=socks5h://127.0.0.1:1080` in its unit),
so that warning is the tunnel being down, not a portal outage.

**WRITTEN, UNCOMMITTED: `scripts/rescan_coverage_gaps.sh`** — runs all six,
splits by tunnel dependency, skips the NE/JU three loudly if nothing answers
on :1080, smallest-first, sequential, `nice -n 15 ionice -c 3`, per-court
timeouts (Bern 4h — RESCAN_ALL re-walks all 11,594 entries, not just the
2,159 missing), `--wait` to hold for the publish lock.

**ALSO STILL UNCOMMITTED: `tools/ne_tunnel.sh`** — its parked diff is
exactly the fix for tonight's failure: restart the proxy if it dies OR stops
answering on :1080, exit if autossh dies so launchd restarts the job. Its
own comment: "No more silent 'tunnel up, proxy dead' state." That is 4 of
the 7 alerts. Commit it.

Expectation-setting on Bern: #68 is filed as *investigate*, not *rescan*. If
2,159 are missing due to a pagination ceiling or portal-side filter,
RESCAN_ALL will return nothing new — and that result IS the diagnosis.

### TODO 2 — cost levers not yet applied (see docs/agent-loop/COST-LEVERS-2026-08-24.md)

Env + timer only, no code:
  - `SWISS_CASELAW_LLM_RERANK_TOP_N=8` in .env.mcp + rolling restart
    (~$138/mo; 7,474 traces show 0.25% top-1 impact)
  - `ocl-ops-dashboard.timer` 60s -> 300s (~$24/mo; we pay Haiku to monitor
    ourselves ~1,323 times/day)
Then the benchmark guardrail (command in the COST-LEVERS doc).

### DEPLOYED AND VERIFIED THIS SESSION

- Export throttle + bulk-access notice (`a0015d66`), then **headers on EVERY
  export response, not only 429** (`08638b1e`) — because the fleet
  self-throttled to ~5.5 req/min/IP against a 10 r/m limit and was serving
  200s, so the 429 notice was never going to reach it. Zero 429s served all
  day; the invitation now rides every response.
  The fleet cut itself from 1,299,445 req/day (08-21) to ~86k/day.
- search-cache maxsize 256 -> 1024 (`3329efbe`). TTL left at 1h on purpose.
- Haiku pricing corrected 0.80/4.00 -> 1.00/5.00 (every prior Haiku figure
  was 25% understated); prompt caching REVERTED — impossible on Haiku 4.5
  (4096-token minimum vs our ~1050).

## 08-24 EVENING — two findings, both measured, both correcting an earlier guess

### 1. QC gate: root cause is WEEKDAY LOAD, not corpus growth (and NOT the fleet)

Every `Duration:` line in publish.log + rotations, 07-30 → 08-24:
**every run over ~1,100s is a weekday; every weekend run is under it.**
Weekends: 748 / 646 / 866 / 686 / 915 / 871 / 781 / 1,047s (n=8).
Weekdays: 681 → 3,519s, incl. 2,477 / 2,658 / 3,248 / 2,993 / 3,137 /
3,519 / 2,680. **Both 3,600s timeouts (08-18 Tue, 08-21 Fri) were
weekdays.** The gate is not outgrowing its budget — it is starved of page
cache by weekday traffic. Step change ~08-10 (weekdays jumped from a
700–1,600s band to 2,500–3,500s) is unexplained and worth its own look.

**A hypothesis that DIED — do not re-run it:** the export fleet was
suspected (08-21 = 1.3M requests = a timeout day). It is not the cause —
the weekday/weekend split predates the fleet by three weeks, and 08-22
had 915k fleet requests with a 781s gate.

**This is a strong argument for the cutover Jonas already wants:** full
build at WEEKENDS only puts the gate in the 650–1,050s band, ~3.5x margin
against the ceiling, instead of the current coin-flip. See
[[qc-gate-headroom]].

### 2. fr_gerichte future dates — real but SMALL, and NOT systemic

18 fr_gerichte records carry `decision_date` in the future — spread over
13 distinct dates 2026-08-25 → 09-15, not one bad batch (an earlier note
saying "all 2026-09-15" was wrong; only 5 are).

Sampled the whole court against the judgment date printed in each
document ("Urteil vom …" / "Arrêt du …"): **13,938 of 14,317 parsed
(97.4%) match EXACTLY**, median delta 0, p90 0. So there is **no systemic
publication-date-as-decision-date bug** — an earlier worry, disproved.
The defect is a 2.6% tail: 178 (1.2%) >7d late, 66 (0.5%) ≤7d late, 135
(0.9%) EARLIER than the printed date. The 18 future-dated ones are the
visible tip of the "late" group. Extremes reach ±8,401 days (~23 yrs),
which look like the wrong date being picked out of the document.

CAVEAT on the 135 "EARLIER" cases: our regex takes the first
"Arrêt du …" in the text, which may be the *appealed* lower-court
judgment — some of those are likely OUR false positives, not corpus
defects. Verify before repairing.

`date_provenance` is NULL for all 14,696 fr_gerichte (and 1,031,376
corpus-wide) — the column exists but is unpopulated outside
`extracted_from_text` (17,667) and `volume_synthetic` (100), so it cannot
be used to audit date origin today. Belongs with the #84/#48 date family.

## 08-24 COST INVESTIGATION — RESOLVED (measured, not inferred)

**Answer to "who is the biggest consumer": claude.ai users.** Anthropic
egress `160.79.106.176–.182` = **$7.29 across 4,006 calls = 32 % of all
attributed spend**, triple the next actor. Not abuse — the product working.

Measured 08-24 (partial day): **attributed $22.72 / 10,206 calls / 753
distinct IPs** (753 vs 332 yesterday — the user base is growing) against
**$33.77** billed. The apparent gap is mostly OUR BUG: the ledger priced
Haiku at 0.80/4.00 vs the real 1.00/5.00, so correcting it lifts the
ledger to ~$27–28 and leaves ~$5–6 = the `internal` query_parse slice that
carries no IP. Fix both and the numbers reconcile; there is no hidden
consumer.

**No second glossagens.** The two heaviest non-Anthropic MCP talkers were
identified from their UAs, and neither is a cost problem:
- `212.25.15.128` (7,415 POSTs) — **an automated poller, not a user**:
  `Go-http-client/2.0`, `POST /` every 10 s exactly, 6 ms, 200. ~8,640
  req/day, ZERO LLM cost (absent from the ledger). Probably an uptime
  monitor or a liveness-badge registry (cf. issue #61). Harmless, but it
  polls `POST /` when `/health` exists for this — worth telling them, and
  it inflates traffic stats. (Earlier note guessing this was the
  repeat-query integrator was WRONG — corrected 08-24.)
- `132.220.78.158` (3,758 POSTs) — **a genuine Python MCP integrator**:
  `python-httpx/0.27.2` → `POST /messages/` 202 (SSE transport), bursts of
  3 in one second. $0.38 over 328 LLM-backed calls today. Well-behaved.

**The search cache is demonstrably working:** the ops-dashboard probe
(127.0.0.1) fell **$1.69 → $0.48, −71 %**, for identical behaviour.

**GET traffic is all crawlers and costs nothing in LLM terms** (223,750
requests in the top 10). The big one is a **FLEET, not one IP** (measured
08-24): ten GCP addresses sharing ONE fabricated `Chrome/120.0.0.0` UA —
34.10.180.98 (44,006), 34.171.68.95 (29,461), 34.56.53.225 (25,735),
34.123.149.87 (14,244), 34.57.162.91 (13,730), 34.61.96.6 (13,051),
136.119.35.120 (10,568), 34.42.229.145 (7,356), 34.30.223.149 (4,874),
35.254.159.230 (4,315) = **167,340 req/day**, bulk-pulling
`/api/decisions/<id>/export.bib` (35,720 of the top IP's 44,006) plus
`/laws/` (7,004) — i.e. **building a CITATION DATABASE of Swiss law**
(BibTeX is the academic format), courts led by ge 8,598 / bvger 7,564 /
ti 7,180 / vd 5,859 / bger 4,315 — heavy on FR/IT cantonal law.

**SUSTAINED, NOT A BURST — ~2.6M requests over 4 days** (zgrep on the UA
across rotated tier1): **Aug 21 = 1,299,445**, Aug 22 = 915,121, Aug 23 =
162,928, Aug 24 = ~207,188. Aug 21 is ~15 req/s sustained for 24 h; the
per-IP nginx limit is 30 r/s, so splitting across 10 IPs kept every single
one legal.

**LEAD WORTH TESTING: 08-21 is also the day the QC gate TIMED OUT**
(started 11:09:20, killed at exactly 12:09:20 on the 3600 s wall clock, no
verdict, cascading into the skipped HF upload + both git pushes). The
recorded diagnosis was "page-cache temperature" and the shipped fix made
timeouts non-fatal — i.e. the SYMPTOM. If 1.3M export.bib reads were
stealing the page cache, the gate was STARVED, not outgrowing its budget.
NOT proven: 08-22 had 915k requests and the gate passed in 13 min. But
"the scan is nearing its ceiling" should not remain the working theory
untested, and [[qc-gate-headroom]]'s climbing p90 may be measuring this
fleet rather than corpus growth. Test: correlate gate duration against
daily fleet volume once the fleet is throttled. rDNS is generic
(`*.bc.googleusercontent.com`) — **unidentifiable by design**; one shared
forged UA across ten IPs = one operator deliberately distributing to
defeat PER-IP quotas. For scale: glossagens, blocked as abuse, did 8,569.
Harm is not the data (CC0) but 167k DB reads/day of page-cache pressure,
incl. during the build window — the same pressure that blew the QC gate
timeout on 08-18. **Per-IP limits cannot fix this.** The fleet's only
fingerprint is that exact stale UA (Chrome 120 is >1yr old; no real
browser fleet sends it). Recommended: `map $http_user_agent` → rate-limit
or 429 on export.bib for that string, with a `Link` header pointing at the
HuggingFace mirror — access preserved, channel corrected. A `deny` is
useless (they rotate); a broad GCP block would hit legitimate users
(34.34.235.x appear in the MCP/cost data and are NOT this fleet).
Then: `216.73.216.39` 30,295 = **ClaudeBot**; `66.249.70.7` 22,438
= **Googlebot**; `136.243.228.177` 18,305 = **DataForSeoBot** (commercial
SEO vendor, returns nothing to the project — a robots.txt Disallow would
test whether it obeys). The spoofed-UA scraper is the one worth acting on:
the data is CC0 and the HuggingFace mirror exists precisely for bulk users
— same call as the 2026-07-14 `deny` ("Bulk users -> HF mirror").

## 08-24 COST INVESTIGATION (Anthropic billing CSV vs our own ledger)

**Billed 2026-08-24 (partial day), OpenCaseLaw key `haiku`: $33.77** —
Haiku $23.95 (in 13.04 / out 10.91), Sonnet $9.82 (in 6.36 / out 3.46).
A separate `senti` key ran $0.50 of Opus 4.6 — different project.
Our own llm_usage.jsonl said **$14.40 for all of 08-23**. Two causes found,
one confirmed, one open:

1. **CONFIRMED BUG — `_LLM_PRICING` was stale.** Haiku 4.5 was priced
   (0.80, 4.00) against a real **$1.00/$5.00**: every Haiku cost in
   llm_usage.jsonl, /metrics and the per-IP ledger was **under-reported by
   20 %**, and Haiku is the bulk of spend. Every daily total quoted from
   those files this week is a FLOOR. Opus 4.7 was 3× too high but unused.
   Fixed (uncommitted).
2. **OPEN — the gap is bigger than 20 %.** Either volume genuinely rose on
   08-24 or some call sites never reach `_llm_usage_log`. Reconcile one
   hour of billing against the ledger to find out.

**Prompt caching: my first recommendation was WRONG — corrected.** The
reference (claude-api skill) states the minimum cacheable prefix is
**~1024 tokens; shorter prefixes silently do not cache**. Of the three
static prompts:
- `LLM_RERANK_PROMPT` ~120 tokens → **ineligible**, and irrelevant anyway:
  rerank input is dominated by the CANDIDATES (LLM_RERANK_TOP_N=15 ×
  FULL_TEXT_RERANK_CHARS=1400 ≈ 5k tokens/call), which vary per query and
  cannot be cached. Rerank is the biggest line (~$5.80/day on 08-23).
- `EXPANSION_SYSTEM_PROMPT` ~250 tokens → **ineligible**.
- `STRUCTURED_PARSE_PROMPT` ~1.2k tokens and ~98 % of that call's input →
  **the only eligible site. cache_control added** (uncommitted).
  It sits just above the 1024 floor, so trimming the prompt would silently
  disable it. **Verify via `cache_r` in llm_usage.jsonl — zero across
  repeated queries means it is not engaging.**
So the real levers are the search-result cache (deployed), shrinking the
rerank payload, and honest accounting — not caching.

**"Who is the biggest consumer" — partially unanswerable today.** On 08-23
the top edge consumers were: `127.0.0.1` $1.69 (**us** — the ops-dashboard
probe, 1/min), `31.164.63.221` $1.50 (**glossagens.ch**, since blocked),
~$1.10 across `160.79.106.177/.179/.180/.182` (**Anthropic egress =
claude.ai users**), `46.225.212.40` $0.28 (**us** — smoke probes).
**But only $7.90 of $14.40 was edge-attributed at all.** `query_parse`
(968 calls, $1.57) logs `source=internal` with NO IP — likely because it
runs in a ThreadPoolExecutor where contextvars do not propagate, unlike
`asyncio.to_thread`. Until that is fixed the ledger cannot answer "who"
for the largest un-attributed slice. Worth a task.

## UNFINISHED 08-24: research-archive expansion (Jonas: "remove the 12 month
## data cap, keep collecting ... as fruitful and useful for research as possible")

**Session ended mid-task — Bash was blocked by a safety classifier, so
nothing below was tested, committed or deployed.** Local edits only.

**Verified facts (do not re-derive):**
- Query text IS in the private repo, one dataset only:
  `/opt/caselaw/data-private/datasets/search_traces/search_traces_<SRCDAY>/<COLLECTDAY>.jsonl.gz`
  — 280,554 records, source days 2026-07-20 → 08-19, committed AND pushed
  (repo confirmed PRIVATE). Schema: candidates, changed, post_top,
  pre_top, **query**, timestamp, type. No session id in traces.
- `datasets/interactions/` does NOT carry query text — stores `query_len`
  (int) only. Leave it alone.
- The "12-month individual-data cap" is published NOWHERE. It exists only
  as descriptive text inside `/opt/caselaw/data-private/TRACES_EXPORT_APPROVED`
  (created 08-19 18:16). Removing it = editing that one file.
- Export was a ONE-SHOT manual run; the systemd unit's ExecStart has no
  `--include-traces`, so nothing has been exported since 08-19.

**DONE, local, UNTESTED (Bash was blocked — none of this ran):**
- `scripts/collect_dev_data.py`: `collect_traces()` is now exactly-once per
  source day (skips today's still-open file; skips source days that already
  have a shard) + `import re`. Without this a daily run re-exports the whole
  30-day window nightly — ~280k records × ~30 duplicate copies/month.
- `tests/test_collect_traces_ongoing.py`: 2 tests pinning both skips. NEVER RUN.
- **`docs/datenschutz/index.html`: all 5 locales amended** (DE/FR/IT/RM/EN
  `priv_summary_bullet_queries`). Dropped the now-false headline "keine
  dauerhafte Speicherung individueller Daten" / "no permanent storage of
  individual information" and the "befristete/limited period" framing;
  added plainly: query texts are retained indefinitely, without session or
  person reference, in an access-restricted private research archive, for
  search quality + research + model training, with no time limit. Kept
  every still-true claim (no IP with requests, no account/login, rotating
  session id, Word doc text never stored, open-source collection code).

**REMAINING (in order):**
1. **VERIFY THE LOCALE DICT PARSES — mandatory before commit.** The 5 edits
   were made without a syntax check because Bash was unavailable; an
   unbalanced `\'` in FR/RM breaks the whole i18n dict and the page fails
   to render. Use the same eval check as the 08-23 IP-ledger amendment
   (extract the `de:{…}` object, `eval` it, print each locale's
   `priv_summary_bullet_queries` prefix; expect 5 locales).
2. Re-read `priv_rollup_never_queries` (~line 229 DE + 4 locale twins) for
   consistency: it ends "Was dauerhaft bleibt, enthält keinen Sitzungs-
   oder Personenbezug." Still accurate (traces carry no session id and no
   IP) but confirm the wording sits right beside the new bullet.
3. `make test` — the two new tests plus the 8 existing collector tests.
4. Commit + push the notice FIRST (GitHub Pages) so it is live before
   collection expands; VPS ff-merge.
5. Marker: rewrite `/opt/caselaw/data-private/TRACES_EXPORT_APPROVED` to drop
   the 12-month cap claim (it is published nowhere — it exists only in this
   file) and state the real basis: indefinite retention in an
   access-restricted private archive for research + model training.
6. Enable ongoing export: add `--include-traces` to the collector unit's
   ExecStart (`systemctl edit` or the unit file), daemon-reload. First run
   backfills 08-20 → yesterday, then one source day per night.
7. Make capture retention an explicit DECISION not a default: add
   `CAPTURE_RETENTION_DAYS=0` to `/opt/caselaw/repo/.env.mcp` with a comment
   (currently unset → code default 0 → never pruned; ~250MB/day on `/`,
   which is at 76%). No worker restart needed for the collector; the
   capture flag is read by the workers, so it rides the next restart.

**Deliberately NOT done (needs Jonas's explicit decision):** exporting
`capture_*.jsonl` to the private repo. It is far richer than traces (77k
records/day vs ~5.8k, every REST+MCP call with full args) and would be the
bigger research asset — but it carries **session ids**, so it is a
step-change in identifiability, unlike the session-free traces. Propose it
as its own decision; do not fold it into "keep collecting".

## TUESDAY 08-25 MORNING — THE CUTOVER VERDICT (do this first)

Jonas asked on 08-24 for **short weekday builds + full build at the
weekend** and approved the staged plan. That is exactly what
`incremental_nightly.py` was designed for. The gate is one command:

```bash
# NB each LINE is one run; the pairs are NESTED under "pairs".
ssh -i ~/.ssh/caselaw root@46.225.212.40 'tail -1 /opt/caselaw/repo/logs/publish_drift_check.jsonl' | python3 -c '
import sys, json
d = json.load(sys.stdin)
print("run:", d["started_at"], " overall ok:", d["ok"])
for p in d["pairs"]:
    print(" ", p["pair"], "ok=", p["ok"])
    for e in (p.get("errors") or []): print("      ", e)
'
```
Status at 08-24 22:00 UTC: the reseed IS running (started 20:00:27;
reference_graph rebuilt in 2,844s exit 0; decision_structure bootstrap
started 20:47 and takes ~4.5h, so it lands ~01:00-01:30 UTC). The newest
drift record is still **2026-08-21** — 08-22's run was killed at the old
6h timeout and Sunday is excluded — so the first post-reseed verdict
appears only after this run finishes. Do not read the 08-21 record as a
verdict on the reseed.
This is the FIRST post-reseed verdict (graph reseeded 08-23; structure
reseeded Mon 08-24 20:00 under the raised 10h timeout). Every record
through 08-21 is pre-reseed and proves nothing.

- **Green/near-green** → one green week, then Stage A: incremental to
  `--in-place` with the full build STILL daily as safety net; then
  Stage B: full build drops to Sunday. Rollback = one timer change.
- **Still red** → do NOT flip. Named suspect first: the −13.2% is almost
  entirely UNRESOLVED `citation_targets` while resolved edges are −1.33%,
  which reads as the full builder recording extraction attempts the
  incremental path discards (semantics, not staleness). Second suspect:
  the BGer↔BGE cross-ref pass (`48d213d4`, FULL builder only).

**Measured cost case (08-23 run, 14h33m):** FTS5 rebuild 5h36m + graph
2h24m + structure 1h54m ≈ 10h of the 14.5h; incremental equivalents are
~30s–5min / 3–5min / 3–5min. ~123 new decisions/day. The weekend build
keeps FTS5 optimize, full Parquet/HF export, and the whole-corpus QC gate.
Pipeline-gated (invariant #5) — Jonas decides at each stage.

## NIGHT 08-23→24 — four items executed (commits b3ec4dbb + a44096ec, deployed)

1. **nginx reconciled + re-symlinked**: enabled (ground truth: ocluniq
   logging + /ops/ block) copied over available, symlink restored, running
   config verified BYTE-IDENTICAL (md5 before/after). Backups in
   /root/nginx-backup-2026-08-24/. Config now versioned at
   deploy/nginx/mcp-server.
2. **ntfy unified**: one env topic (see /opt/caselaw/ops.env) feeds
   quota alerts + scraper-freshness + output-freshness + anomaly-report +
   bger-poller via EnvironmentFile drop-ins; scripts keep legacy topics
   as fallback; 3 tests. Jonas subscribed + device-verified.
3. **oai_pmh day-atomic fix (a44096ec)**: unshrinkable/lying/erroring
   windows are force-walked whole (bounded attempts + _FORCE_MAX_PAGES)
   then recorded as gaps — a source never aborts for one window; install
   guard extends to (aborted or gaps) and total <= prev. 4 new tests;
   SONAR sub-day path untouched. Suite 2119/22/0.
4. **Out-of-band backfill LAUNCHED** (~23:30 UTC, /root/backfill_20260824/):
   sequential ZORA → UNIGE, timeout 6h each, nohup. ZORA immediately
   exercising the day-atomic path on its own mass-restamp days.
   **MORNING CHECK:**
   ```
   ssh root@46.225.212.40 'grep -E "=== |Done |gaps" /root/backfill_20260824/run.log | tail -12'
   ssh root@46.225.212.40 'wc -l /opt/caselaw/repo/output/legal_scholarship/{zora_law,unige_law}.jsonl'
   ```
   Expect zora_law ~9-10k, unige_law thousands (>1,115), gap lists small.
   THEN post-window (~17:00): rebuild legal_scholarship.db via the
   standard builder, re-test #89's repro queries. Next Sunday's step 2h
   inherits the fixed code with a week of evidence.

## LATE EVENING 08-23 — abuse case closed + alerting channel

- **31.164.63.221 BLOCKED at nginx** (Jonas's decision after the ledger
  review): glossagens-audit/1.0 (glossagens.ch, Sunrise xDSL), 4,229
  verify_claim attempts against the 200/day quota, all-day retry loop, no
  backoff. `deny` added next to the 2026-07-14 precedent in BOTH
  sites-available/mcp-server AND sites-enabled/mcp-server — **finding:
  sites-enabled is a DIVERGED REGULAR FILE since Aug 2, not a symlink**
  (word-addin is a proper symlink); reconcile + re-symlink is an open
  chore. Lift the deny if outreach ever lands.
- **ntfy operator channel minted**: `opencaselaw-ops-ef35236fe4d9`
  (old default `opencaselaw-prod` was guessable AND Jonas was never
  subscribed — poller pushes went nowhere). Wired: .env.mcp (workers,
  picked up at next restart) + bger-poller drop-in (daemon-reloaded).
  Test push sent, waits in topic cache. Jonas must SUBSCRIBE in the ntfy
  app. check_scraper_freshness.py still hardcodes its own topic
  (opencaselaw-scrapers) — env-ify later, content harmless.
- **Quota-alert delivery FIX built, uncommitted** (5th commit candidate):
  web_api/ocl_quota.py pushes to NTFY_TOPIC exactly once per
  (day,endpoint,ip) breach via INSERT OR IGNORE rowcount; push contains
  NO IP (public topic). + tests/test_quota_alert_delivery.py (4 tests).
  Suite 2112/22/0. Root cause: quota_alerts was write-only audit; the
  "team@" address is the CLIENT-facing higher-limits mailto, not
  operator alerting.
- Ledger day-1 review findings: 25% of edge spend is self-monitoring
  (ops-dashboard 1/min probe via loopback — already being absorbed by
  the new search cache, 60/h → ~8/h steady state; smoke via own public
  IP). 127.0.0.1 is quota-ALLOWLISTED but still ledger-logged — label
  or exclude self-traffic in ledger analyses.

## SESSION 2 VERDICTS + WORK (written 2026-08-23 ~10:20 UTC, supersedes the checks below)

**All four Sunday-morning verdicts GREEN, live-verified ~10:00 UTC:**
- **Gate:** real verdict 09:55:49 after ~17.5 min (started 09:38:22) — passed,
  WARN path present but not needed on its first live run. Runtime keeps
  creeping (13 → 17.5 min); the incremental-gate medium-term item stands.
- **Homepage sync FIRST LIVE RUN WORKED:** "updated: #bignum -> 1'054'225,
  #f-decisions -> 1'054'225, #trust-delta -> 19, #trust-date -> 2026-08-23";
  docs/index.html rides the bot's stats commit as designed.
- **IP/cost ledger:** accumulating, 277 lines at ~09:50 UTC.
- publish.log logrotate fired its first rotation (102M gz archived; live log 13M).

**LRU search-result cache BUILT + tested locally, NOT committed (queue item 1).**
`mcp_server.py` +72: `_SEARCH_RESULT_CACHE` (_BoundedTTLCache 256×3600s) wraps
the full search_fts5 body — FTS5 + hybrid + Haiku rerank — keyed sha256 over
ALL args serialized byte-exact (FTS5 operators are case-sensitive; a folded key
would partially undo c975ea1a). Deep-copy both directions; meta replayed via
always-dict inner meta; limit ≤ 100 gate; deadline_partial results never
cached; cleared by _cache_clear() on db_generation swap; hits/misses/entries
in /metrics `search_result_cache`. Accepted trade, noted deliberately: a
rerank-TIMEOUT ordering (invisible at the seam) can live up to the 1h TTL;
cache hits also emit no rerank trace (a hit's trace would duplicate its
miss's) — repeat-rate analysis reads the new counters from now on.
New: `tests/test_search_result_cache.py` (12 tests) + `tests/conftest.py`
(global cache isolation — fixture DBs share user_version=0 so the swap hook
never fires between tests). Suite 2098 passed / 22 skipped / 0 failed.
Suite runtime drift: ~75s on two runs (target ~60s, CLAUDE.md) — grew with
the 1898→2098 test count this week, not with this change (conftest import
cost is once per process); worth a look if it keeps climbing.
**Commit boundary when approved:** exactly `mcp_server.py tests/conftest.py
tests/test_search_result_cache.py` (stragglers stay parked). Deploy: commit →
push → `merge --ff-only` on VPS → rolling restart (module-level code; workers
do NOT pick it up per-call). Verify 30-60 min post-restart: `search_result_cache`
hits + search p50 in /metrics — per-worker ×8, so hit rate ramps as each
worker warms; the integrator loop (~68s cadence) is the natural warmer.

**IP-exposure audit (Jonas request, 08-23 ~12:00): NOTHING PUBLIC — verified.**
Ledger gitignored+untracked dev AND VPS; llm_usage.jsonl IP-free by design;
output/quota.db unserved+untracked; nginx's only fs alias is /ops/ behind
basic auth; /metrics live-checked: class labels only, zero IPs; tier1 72h +
HLL unchanged. **Jonas DECIDED (AskUserQuestion): raw ledger shards go into
opencaselaw-data — accepting permanence over the previous 90-day-only
posture.** Implemented, uncommitted: (a) /datenschutz/ ×5 locales amended —
"90 Tage" → "90 Tage auf den Serversystemen; zusätzlich zugriffsbeschränktes
privates Archiv für die Dauer des Betriebs", purpose gains "Dokumentation";
(b) collect_dev_data.py: IDENTIFIER_EXEMPT_DATASETS = {"llm_cost_by_ip"}
(single greppable carve-out; scan intact for everything else), dataset
llm_cost_by_ip with bootstrap-from-LEDGER_ARCHIVE_SINCE="2026-08-24" —
the first full post-amendment DAY, so every record written before the
amendment was live (incl. all of 08-23 up to the push moment) stays under
the old 90d promise and is never archived — then daily increments;
REVERSIBILITY: until the first archived records age off the VPS
(~late Nov), the archive can be undone by deleting the dataset dir +
history rewrite of the small private repo; after that, git-history
surgery is the only path back; (c) tests/test_collect_dev_data.py: 3 new tests incl.
pinning the exemption set to exactly one member. 8/8 pass; collector
dry-run clean. **Sequencing on deploy: the SAME push must carry the
datenschutz amendment (GitHub Pages) and the collector change (VPS
ff-merge) — the amendment must not land after the first archive commit.**
First archive: the FIRST TIMER RUN AFTER DEPLOY (tonight's timer runs the
old code — correct, nothing lost: archive-since is 08-24), or hand-run
`python3 scripts/collect_dev_data.py --dest /opt/caselaw/data-private`
after the ff-merge. Residual flagged to Jonas: the dev-data summary bullet
("Dauerhaft bleiben nur aggregierte Statistiken…") is scoped to
Entwicklungsdaten but reads broadly; his legal call whether to reconcile.

**Commit boundary (THREE commits when approved; suite green at 2106/22/0):**
1. cache: mcp_server.py (cache hunks) + `tests/conftest.py
   tests/test_search_result_cache.py`
2. ledger archive: `scripts/collect_dev_data.py docs/datenschutz/index.html
   tests/test_collect_dev_data.py` (+ this handoff file)
3. dedup Phase-4 resolver + benchmark fix: mcp_server.py (manifest hunks:
   REPRESENTATION_MANIFEST_DB_PATH, _get_manifest_conn, _representation_info,
   get_decision wiring) + `tests/test_representation_links.py
   benchmarks/pinpoint_regeste_bench.py`
   mcp_server.py is shared between commits 1 and 3 — split by hunk
   (`git add -p`) or commit sequentially; the hunks don't overlap.
   Post-deploy for 3: verify on a live GE pair (e.g. get_decision of a
   ge_gerichte ACAPJ id → Representations block), then re-run the pinpoint
   benchmark pair.
4. small fixes (evening batch, suite 2108/22/0):
   `docs/mcp/index.html` — tool count 41→42 in all 12 strings (5 locales +
   static + meta description was already bumped externally) AND the missing
   list entry added: the drifted tool is `get_decisions` (diffed the page's
   list against the live server's 42);
   `scripts/build_demand_queue.py` + `tests/test_demand_queue_artifacts.py`
   — URK_ (= "Urk. N" Zurich EXHIBIT refs misread as dockets; top-2 queue
   rows, 1,816×/1,158×, all zh_sozialversicherungsgericht) + control-char
   refs filtered with logged exclusion counts; extractor root-cause fix
   stays pipeline-gated;
   `docs/outreach/2026-08-23-unil-serval-oai-fr.md` — UNIL/SERVAL OAI
   outreach letter DRAFTED in jh-voice (FR, first-contact register),
   recipient address to verify before sending; sending needs Jonas.

**HARVEST VERDICT (Step 2h ran 14:31–15:34 UTC): MISS on both headline
sources — three stacked causes, corpus unchanged (nothing lost, nothing
gained).** zora_law 0 (never started), unige_law kept at old 1,115.
1. **Step budget starvation:** the whole multi-source harvest shares ONE
   3600s wall-clock (`timed out after 3600s` at 15:31:38). The 08-22 plan
   said "expect 1.5-2.5h longer" — the cap vetoes that expectation by
   design. ZORA (218k-id scan, ~40-60min alone) never got a turn.
2. **UNIGE poison window:** repeated `cannot bisect further — marking
   harvest aborted` on 6h windows around 2023-03-14/15 — their OAI errors
   persistently there; bisection hit its floor and aborted the SOURCE
   instead of skipping the window. Then the abort detector correctly
   refused to replace the old file (safety worked; 13-year gap remains).
3. zora_law.jsonl existed empty → "ingested 0 rows" (no prior zora data,
   so no regression).
**Fix plan (pipeline-gated, needs Jonas):** (a) run the two FIRST FULL
harvests out-of-band post-pipeline (one-off `scrapers/scholarship` run
against ZORA + UNIGE, atomic install, then nightly does increments only);
(b) oai_pmh.py: on bisection floor, skip+record the window as a gap and
CONTINUE the source instead of aborting it; (c) optionally per-source
budgets inside step 2h rather than one shared 3600s.

**UNIGE root cause NAILED (probed live 18:54 UTC, 7 polite requests,
project UA):** their OAI *declares* `YYYY-MM-DDThh:mm:ssZ` granularity in
Identify but *silently breaks* on sub-day windows — the failing
[2023-03-14T12:00..17:59] window reports completeListSize=16,706 yet
serves the GLOBAL list from record id 1 (datestamp 2008-10-29). Day
granularity works correctly (2023-03-16 returns proper different ids).
2023-03-14 holds 16,706 records = a mass re-index/restamp day (platform
migration). Death spiral: 16.7k-record day → mid-walk error (their
resumptionToken TTL ~6 min) → bisect to sub-day → every child returns the
same broken global list → "cannot bisect further" → whole source aborted.
**Fix shape for oai_pmh.py (b, refined):** treat a day as ATOMIC when
sub-day children misbehave (child serves ids outside its range or
child completeListSize == parent's) — retry the full-day walk instead
(16.7k ≈ 167 pages ≈ 3 min at 1 req/s, feasible); unfixable days →
recorded gap + CONTINUE; never abort a source for one window. The
sub-day-gating-on-Identify logic was correct by spec; the server lies. The e-periodica /
LIBRA / sui_generis / leoh sources all harvested + replaced cleanly —
the new windowed harvester works; the failures are budget + poison-window
policy. #89 re-test stays deferred until the corpus is actually enriched.

**#49 SIZED (post-window scan, 18:5x UTC, /root/bleed_scan_20260823/):**
50,481 BGE scanned with a verbatim replica of the serving detector over the
first paragraph. **True page-bleed class: 7,674 records (15.2%) open with a
bare page-number line**, concentrated 1875–1960s; sample bge_1_I_26 verified
as the full pattern (page header + PREVIOUS case's dispositive + Fraktur-
garbled OCR before this case starts). Repair sketch: strip leading page-
header/running-head lines + boundary-detect the prev-case tail before the
"N. Urteil vom DATE" header at structure-extraction time; pre-1900 Fraktur
OCR quality is a separate (Tier-1-scale) re-OCR question. **The scan's
language-mismatch leg (5,370) is NOT bleed** — sample-verified as French/
Italian judgments ("Extrait de l'arrêt…") under de-tagged records: the BGE
`language` field means Regeste-language, not body-language, for ~5.4k
records. Separate finding: affects language filters + any body-language
assumption; worth its own task. Serving detector itself unaffected (its
15-token + ×2-dominance floor doesn't trip on these in the rule_statement
path). Benchmark note: pinpoint_regeste_bench crashed on schema drift
(`no such column: citation_count` at :164) — fix queued locally, re-run
post-deploy.

**Queue after this:** harvest verdict ~14:00 UTC (zora first ~9-10k, unige
unfrozen, unifr/usi) → #89 re-test → semantic-rescue benchmark (post-pipeline,
~17:00 UTC) → dedup consumption → the rest as listed below.

## NEXT SESSION — START HERE (written 2026-08-23 09:40 UTC)

**Pending verdicts, check in this order:**
```bash
# 1. Sunday run: gate (first live timeout≠regression), homepage sync (first live run)
ssh root@46.225.212.40 'grep -E "Step 5c \(|DEGRADED|homepage sync" /opt/caselaw/repo/logs/publish.log | tail -4'
# 2. Harvest (~14:00 UTC, Step 2h weekly): zora_law first harvest (~9-10k expected),
#    unige_law unfrozen (1,115 → thousands, post-2013 French), unifr/usi new
ssh root@46.225.212.40 'grep -E "(zora|unige|unifr|usi)_law" /opt/caselaw/repo/logs/publish.log | tail -8'
# 3. Monday 20:00: structure-shadow reseed re-fires (10h timeout now). Tuesday: drift verdict.
# 4. New IP/cost ledger accumulating: logs/llm_cost_by_ip.jsonl (90d) — first
#    weekly review answers "who spends what" in one groupby.
```

**Shipped 08-23 (all live-verified):** /search/ overhaul + capabilities
panel (5 locales) + explicit-OR fix + ATF/DTF lookup + "judgment of" frame;
PR #91 reviewed (request-changes: Zed correct, Continue/OpenWebUI configs
wrong, 2 tool-param errors, 41→42); IP/cost system (quotas on MCP Sonnet
tools, source attribution, 90d per-IP ledger, /datenschutz/ ×5); embeddings
SUCCESS (303,387 vectors); graph shadow reseeded; devdata insights (LRU
cache justified: ~30% repeat rate incl. one integrator loop at 8,913×/wk;
demand_queue = pre-2007 BGer/EVG gap, 160k dangling edges, top-2 rows are
URK_ extraction artifacts).

**Queue:** LRU search cache (evidence in hand) → #89 re-test after harvest →
semantic-rescue benchmark (embeddings fresh, flag off) → dedup consumption →
UNIGE-fix verification → demand-queue URK_ artifact fix → PR #91 follow-up →
IRIS outreach letter. Backup stays deprioritised (Jonas, 08-22).


## MORNING 2026-08-23 — VERDICTS (read before the four checks below)

- **Embeddings: SUCCESS, 02:24 UTC** — 303,387 new vectors over 9.17M rows,
  swapped atomically. paragraph_embeddings.db is CURRENT. The
  semantic-rescue benchmark is unblocked. (Delta was 6× forecast because
  the structure rebuild renumbers paragraphs corpus-wide — recurs on every
  structure-extractor change; consider content-hash keying later.)
- **Graph shadow: RESEEDED** — the version trap fired
  (`version_mismatch:…:1`), full bootstrap 3.4h, exit 0, 1,054,206
  decisions seeded with current extraction (ATF/DTF in).
- **Structure shadow: NOT yet** — systemd killed the service at its 6h
  start-timeout (02:01:14) mid-structure-bootstrap. Self-heals Monday
  20:00 (structure still stores '2' → trap re-fires, ~4.5h run).
  TimeoutStartSec raised to 10h (drop-in deployed + in repo) so a double
  bootstrap can never hit the ceiling again. **No drift record was written
  last night** (killed before the check) — first meaningful drift verdict:
  Tuesday morning, graph pair should collapse toward green already in
  Monday night's record if the check runs after step 2.
- Sunday's nightly launched 03:30 on schedule; harvest (Step 2h) ~14:00,
  homepage-sync first lines ~09:45 (Step 6a), gate ~09:45.
- **`/search/` overhauled (`d388f388`)** after a user-uselessness report.
  Diagnosis: solid backend, wasteful page — "(untitled decision)" on every
  BGE (they carry no title; identity = citation + Regeste), court codes
  instead of names, and the API's is_leading_case / citation_count /
  snippet / pinpoint fields fetched then discarded; the example docket
  6B_1234/2025 was fabricated. Now: derived headlines (abbreviation-aware
  splitter — "Art." must not end a sentence), Leitentscheid + N×-zitiert
  badges, →E. x.y pinpoint deep links, BGE/BGer + newest-first filters
  (API supported both all along), "2'000+" lower-bound totals. Verified
  against live payloads pre-push; deploys with the docs/ push.
  Remaining search-page ideas: date-range inputs, cantonal filter
  (needs a court-code group param API-side), KWIC highlighting.
- **Capabilities panel added to /search/ (`456b10c0`), five locales,
  every claim live-verified first** — incl. NOT (99→79), sort=date_desc,
  court=bge, and the trilingual-Regeste cross-language effect
  ("responsabilité du détenteur" → BGE 131 III 115 rank 1). Ledes
  softened from "every published decision" to "1M+" (README-honest).
  The verification CAUGHT an overclaim before it shipped: the FR text
  promised ATF lookup while /api/lookup rejected ATF/DTF prefixes —
  fixed (`ee8fc623`, #43 REST slice), 6 tests, deployed + rolling
  restart + live-verified. Pattern held: verify the claim, not the hope.
- **Second sweep of the panel's operator claims found explicit OR broken
  on the decisions path** ('Mietzins OR Pachtzins' → 52, fewer than either
  operand: the classifier said operator, _sanitize_fts5 quoted it, the
  explicit branch ran the sanitised string). Fixed (`c975ea1a`) by deriving
  the explicit raw strategy from the ORIGINAL via _explicit_laws_match —
  the same #60 cure search_laws got. Live-proven: 6 of top-20 results are
  single-operand docs; collision (#88) and NOT regressions guarded; 5
  tests; suite 2081. **Residual:** explicit-OR `total` counts the fused
  pool (~110-131) not the true union (~2500) — correctly flagged
  total_is_lower_bound so the page shows "110+", but a real COUNT for the
  explicit path would be truer; same family as #75's counting work.

## MORNING 2026-08-23 — start here (fresh session, context cleared)

Four checks, then the day's work sorts itself:

```bash
ssh root@46.225.212.40 'tail -3 /var/log/opencaselaw-embeddings-topup.log'
#   → "SUCCESS — swapped": embeddings current; queue the weekly timer + the
#     semantic-rescue benchmark (see semantic-rescue-state memory).
#   → cutoff/failure: live DB untouched; rerun /root/embeddings_topup.sh (resume-safe).

ssh root@46.225.212.40 'tail -1 /opt/caselaw/repo/logs/publish_drift_check.jsonl' | python3 -m json.tool | grep -E '"pair"|"ok"|drift'
#   → green/near-green: the 08-22 reseed worked; green-week clock runs, flip ~Aug 29.
#   → still far red: the reseed missed something — first suspect the BGer↔BGE
#     cross-ref pass (48d213d4, full builder only); see shadow-cutover-state memory.

ssh root@46.225.212.40 'grep "homepage sync" /opt/caselaw/repo/logs/publish.log | tail -2'
#   → first live run of the step-6 wiring (67698d11). Expect "updated: …" or
#     "already current". Also confirm docs/index.html rides the bot's stats commit.

ssh root@46.225.212.40 'systemctl status opencaselaw-smoke --no-pager | tail -3; cat /opt/caselaw/repo/state/last_publish_success.json'
#   → smoke green; success marker from 08-23's run. Check non_fatal_failures:
#     a "5c … WARN" entry means the corpus shipped UNVERIFIED (new semantics).
```

Then the queue, in order: semantic-rescue benchmark (two commands, decides
PINPOINT_SEMANTIC_ENABLED with numbers) → dedup consumption design (serving
collapses the 140,912 mapped duplicates) → #48 date-repair pass (sized below:
11% padded, 10.3% volume-contradicting, batch mode visible) → green-week
watch. Backup remains explicitly deprioritised by Jonas (2026-08-22).

## Scholarship source expansion (#89 option 3) — probed live 2026-08-22 evening

Production corpus: 25,073 records / 24 sources, **French only 11%** (2,868).
The registry (`scrapers/scholarship/sources.py`) already scaffolds most
candidates. Live probe results:

1. **ZORA is harvestable again.** The 2026-05-27 blocker ("OAI returns 504")
   is gone: `www.zora.uzh.ch/server/oai/request` answers Identify AND
   ListIdentifiers (completeListSize **217,930**, 100/page ≈ ~40-60 min at
   the configured 1 req/s). Registry entry `zora_law` is fully configured,
   `active=False` → flip it. Expected ~9-10k law records (BORIS yielded
   7,319 from a 167k IR with the same filter). Largest law faculty in CH.
2. **UNIGE harvest is frozen at 2013 — OUR bug, not theirs.** Their OAI
   serves 2026 records fine (verified `from=2026-06-01` → 100+ ids). 867
   French records ending 2013 means the harvest died partway and never
   resumed — 13 years of Geneva law (the top French faculty) missing.
   Diagnose the resumption/cursor state in `oai_pmh.py`. Single biggest
   French win available.
3. **FOLIA = SONAR.** `folia.unifr.ch/oai2d` answers as "Swiss Open Access
   Repository" (Invenio) with global sets. So Fribourg = `sonar.rero.ch/oai2d`
   with `set=unifr` — resolves `folia_law`'s "endpoint TBD". SONAR also
   carries `usi` (Università della Svizzera italiana — would be the FIRST
   Italian academic source; corpus has ~140 it records), `hesso`, `fernuni`.
   One endpoint, several sources, existing `oai_pmh` kind — config-only.
4. **IRIS (UNIL/SERVAL) still closed:** `/server/oai/request`, `/oai/request`,
   `/server/api` all 404. Registry's "needs outreach to UNIL library" stands —
   a jh-voice email is the unblock. Lausanne = second French law faculty.
5. **`justice` (Justice-Justiz-Giustizia, trilingual judges' journal):**
   registered `kind=custom, custom_module=scrapers.scholarship.justice` but
   **the module does not exist on disk** — real scraper work, and license is
   OA-no-redistribution (metadata + abstract + link only).
6. **e-periodica language backfill:** 6,985 records (28% of corpus) have NULL
   language, so French/Italian content there is invisible to language
   filters. Data-quality fix in the e-periodica mapper.

Order of value for #89's French gap: UNIGE-fix ≥ SONAR(unifr) > ZORA (volume,
mostly de) > IRIS outreach > justice module > e-periodica backfill.

**EXECUTED same evening (`944e4bed`, deployed to VPS):** windowed OAI
harvesting (completeListSize-verified, probe-and-bisect, sub-day splitting
gated on Identify granularity, retry×3, atomic install) + registry: ZORA
activated, unige windowed, unifr_law/usi_law added via SONAR. Live-verified:
UNIGE smoke yielded 206/220 records datestamped >2013; truncation detector
fired in the wild. **Sunday 08-23's Step 2h runs the first full harvests —
expect it to run 1.5-2.5h longer than usual** (ZORA scans 218k records,
UNIGE ~124k windowed). Verify Monday: per-source counts in
`build_legal_scholarship` output — unige should jump from 1,115 toward
several thousand with post-2013 French content; zora_law appears for the
first time (~9-10k expected). Then RE-TEST #89's two repro queries against
the enriched corpus before touching ranking. Remaining: IRIS outreach
letter (jh-voice), justice module, e-periodica language backfill.

**FR/IT/RM follow-up probes (late 08-22, all live-verified):**
- **e-periodica backfill upgraded from cosmetic to material**: ZSR (=Revue
  de droit suisse = Revista da dretg svizzer) and the trilingual
  criminology cluster are ALREADY harvested (1,127 + 1,011 records via the
  ddc sets) — their French/Italian articles sit in the corpus with NULL
  language. The backfill makes existing content discoverable; no new
  scrape needed.
- **OpenAlex as a CC0 metadata layer**: CH-institution + Law-concept,
  OA-only: fr 1,006 / de 916 / it 75 works with DOIs. Includes UNIL output
  → bypasses the closed IRIS. New REST harvester (cursor pagination),
  dedupe by DOI against existing sources. Caveat: OpenAlex language/concept
  tags are noisy; treat as metadata+link records.
- **SONAR quick adds**: sets `fernuni` (UniDistance — has a law faculty,
  FR/DE) and `hesso` (droit économique) — one-line entries on the endpoint
  verified tonight.
- **Romansh is real and it is in the LAW, not scholarship**: Fedlex SPARQL
  counts **237,891 ROH-language expressions** (raw; refine to consolidated
  acts before sizing). Extend build_statutes_db to ingest RM versions —
  the schema already reserves 'rm'. Second RM+IT lever: GR lexbrowser
  publishes the Bündner Rechtsbuch in DE/IT/RM — the GR cantonal-law
  scrape could carry all three (also strengthens Italian via the Grigioni).
  Mind [[cantonal-laws-build-inputs]] before any cantonal_laws rebuild.
- **DOAJ sweep method validated** (sui generis found under country:CH);
  a systematic CH-law-journal enumeration is a 30-min follow-up.
- Outreach-class candidates (license permission needed, metadata+links
  model): LawInside.ch, cdbf.ch (FR case notes); Rivista ticinese di
  diritto (IT).

**PR #91 reviewed (request-changes posted, 2026-08-23):** external
contributor's MCP setup guides for Continue/Zed/Open WebUI, closing #71.
Zed guide verified CORRECT against Zed docs (ship as-is). Blocking:
Continue's primary block is Claude-Desktop's format (real shape:
config.yaml mcpServers list — verified against Continue docs); Open
WebUI's "Valve / MCP Connections" menu appears invented (docs still
route via mcpo proxy); both tool examples use nonexistent params
(get_law "law"→abbreviation; find_leading_cases "statute"→law_code+
article); "41 tools" should be 42. Offered to apply fixes on merge if
the contributor doesn't iterate. **Separate pre-existing bug this
surfaced: docs/mcp/index.html's own strings say "41 tools" in all five
locales — live is 42; fix independently of the PR.**

**IP/cost collection live (08-23, `9c92b9da`):** per-IP daily quotas on
MCP check_claim_support + grounded attest (shared REST buckets, throttle →
team@jonashertner.com); llm_usage gains source+client; 90-day per-IP cost
ledger `logs/llm_cost_by_ip.jsonl` (logrotate maxage 90 = the retention
promise); REST middleware stamps provenance. /datenschutz/ amended ×5
locales ("Cost and abuse control (90 days)"). Both transports live-verified
in the ledger. Tier1 (72h access.log) unchanged.

Issues closed evening of 08-22 after live verification: **#88** (article
resolution, twice-verified at scale), **#75** (small-limit lower-bound marker
now present in the reporter's exact repro), **#69** (get_doctrine 0.3s vs the
20s threshold). **#78 is 2.5/3 fixed** — OR-as-operator passes the reporter's
220=220 proof and NEAR(a b, N) works, but the DOCUMENTED `NEAR/5` form still
silently no-matches. Small queued fix: translate `NEAR/N` → `NEAR(a b, N)` in
the sanitiser AND align the schema text, then close. 19 issues remain open.


## Incident: search 500s, 12:41–13:24 UTC (self-inflicted, resolved)

The `#89` weak-match helper shipped in `4f8c1cdd` was named `_term_coverage`.
That name was already bound at line 7173 by the search reranker's scoring
function, which `_rerank_rows` calls five times. Python keeps the last
definition, so from the 12:15 rolling restart every reranked search called a
function with the wrong signature. **458 tracebacks; `search_decisions` and
`/api/decisions?query=` returned 500.** Hotfix `4456f1c6` renamed the new one
to `_scholarship_term_coverage`; verified restored.

Three things to carry forward, in order of importance:

1. **No monitor would have caught it.** `quality/smoke.py` probed health, one
   decision page, one export and publish freshness — nothing that ran a query.
   It was found because a human ran `make smoke` by hand. Fixed in `7e641ec9`:
   there is now a `search_query` probe, and `publish_freshness` is build-aware
   (it had been red ~13h of every day, so it was *already* alarming during the
   outage and would have added no signal).
2. **The suite passed 2003 tests either way**, because nothing called
   `_rerank_rows` — the hottest path in the product had zero coverage.
   `tests/test_rerank_rows_smoke.py` now executes it;
   `tests/test_no_shadowed_definitions.py` catches the collision structurally.
   Both verified against a scratch copy carrying the real bug: 13 failures with
   it, 21 passes without.
3. **Two definitions 4,800 lines apart in a 22,000-line module are invisible to
   review.** The AST guard is the only thing that scales here. If `mcp_server.py`
   is ever split, keep that guard pointed at whatever replaces it.


Session covered the GitHub issue batch #81–#90 (filed by Tomagori 2026-08-20),
plus operational findings. Written at the end of the session; verify live state
before acting.

## Do this first

**RESOLVED 2026-08-22 11:30 UTC.** The seven fixes are committed (`4f8c1cdd`)
and pushed; the VPS fast-forwarded to `9bbd2349` and its `mcp_server.py` is
tracked-clean at sha `2bd2c93b85cd36ca`, matching the committed file. No worker
restart was needed — the delta from the deployed file was one comment block.
Workers last started 09:55/10:02 UTC, before the swap window, so none booted
pre-fix code.

Original problem, for the record: production ran code that existed in no git
history, scp'd into the git tree against the `mcp-deploy-path` rule, where a
conflicted nightly `stash pop` would have reverted it silently.

Two things learned doing it, now in the `mcp-deploy-path` memory:

- `git pull --rebase` **fails on the VPS** — the tree permanently carries ~68
  dirty files and rebase demands a globally clean tree. Use
  `git merge --ff-only origin/main`, which only touches the files it updates.
- The MEMORY.md index line for `mcp-deploy-path` said the opposite of the
  memory ("deploy by scp … never git push"). That inverted one-liner is what
  loads into every session. Corrected.

Backups: pre-fix `/opt/caselaw/repo/mcp_server.py.bak-2026-08-22-pre-issue-fixes`,
as-deployed `/root/mcp_server.py.fixed-2026-08-22` (sha `8fb5893be02d859c`).

## Issue state

*Second pass, 2026-08-22 ~12:30 UTC: deployed as `a7a6ac38`, all eight workers
rolling-restarted, every claim below re-verified against live production.*

Closed, fixed, verified in production: **#81, #82, #83, #86, #87**.

Open with replies posted:
- **#85** — the reported cause (BM25 gap) was *partly* wrong: the gap gate does
  fire (gap_ratio 1.131 < 1.2 on repro A), but token coverage is the binding
  one and it stays suppressed even at a gap of 1.6. Shipped the honest
  diagnosis (response now names the gate). The absolute floor the reporter
  asked for is NOT done and needs the search benchmark. **Still the main open
  search item.**
- **#88** — **now fully fixed.** Correction to the first-pass note: the query
  did not return "nothing", it returned five articles that merely *mention*
  Art. 60 OR. Root cause was structural, not an OR problem —
  `_abbreviation_lookup_federal` rejects any query containing a space, so an
  article reference never reached it and fell through to bag-of-words BM25,
  where a provision cannot win its own query. `_article_reference_lookup_federal`
  now resolves DE/FR/IT reference forms to the named article at rank 1. This
  was internal task #50, so **#50 is done**.
- **#89** — the stopword fix stays off (measured no-op at 25k scale). Shipped
  the weak-match signal instead: term coverage on the top hit, reported and
  never used to reorder. Ranking is still wrong; it is now honest about it.

**#84** — serving-side guards shipped, corpus repair still open:
- `rule_statement` page-bleed now suppressed with an explanation
  (internal #49's cheap detector: page-number opening line, or prose in a
  language the record is not in). Screened on the `full_text` fallback only —
  a Regeste is legitimately trilingual and screening it would strip the field
  from every BGE. Same-language bleed still passes.
- `decision_date_warning` on `cite` when a BGE's stored year contradicts its
  volume (vol. N = year N + 1874, ±1). Internal #48's detector.
- `attest_response` no longer blames the draft when the *stored* date is the
  outlier — new `stored_date_suspect` problem type. Before this, dating
  BGE 76 II 346 correctly to 1950 was reported as a hallucination.
- Still open: the wrong dates and bled `full_text` in the corpus, and the
  missing Regeste. Run the volume-year rule across the whole BGE collection to
  size the defect — that is a scan, so it waits for the build window.

**Two leads for that scan, both cheap:**
1. `cite("BGE 105 Ia 349")` returns `decision_date: "1979-01-01"`. The year is
   right, but 01-01 is almost certainly a year-only date padded to day
   precision. `COUNT(*) WHERE court='bge' AND decision_date LIKE '%-01-01'`
   sizes that class in one indexed query and probably explains a large share
   of the "wrong date" reports without any volume arithmetic.
2. The volume-year rule itself (vol. N = year N + 1874, ±1) is now in
   `_bge_volume_year_mismatch` and runs on a citation string — so it can be
   applied to a `decision_id` list without touching `full_text`.

**Trap for anyone scripting against prod:** `mcp_server.STATUTES_DB_PATH`
defaults to `/root/.swiss-caselaw/statutes.db`, a stale February copy holding
**36 laws**. The workers get the real 5,528-law mirror only via
`SWISS_CASELAW_DIR=/opt/caselaw/repo/output` from their systemd unit. An
ad-hoc script on that host reads the stale DB and reports confident nonsense —
it produced three false MISSes during this session's validation before being
caught. Export `SWISS_CASELAW_DIR` explicitly.

Open, no reply, corpus-side and blocked on the build window: **#90**.

Also: **#78** improved as a side effect of the #88 work (both route through
`_has_explicit_fts_syntax`); `search_practice("Arbeitszeit OR 321")` now treats
`OR` as a literal. Not verified for the operator half, no comment posted.

Nine documentation issues (#62–#67, #70, #71, #73) have had no reply since
2026-08-10.

## Work in the tree, uncommitted

`mcp_server.py` (+~400), `search_stack/build_statutes_db.py` (+29),
`tests/test_statutes_article_num.py` (+49), and 8 new test files (71 tests).
Suite: **1975 passed, 22 skipped, 0 failed** (baseline before any edit: 1898).

`build_statutes_db.py` is NOT deployed and takes effect only on a statutes
rebuild — that rebuild also retires the two-pair alias in `get_law`.

## Internal tasks filed this session

- **#48** BGE `decision_date` vs volume-year rule (volume = year − 1874)
- **#49** `rule_statement` page-bleed from the preceding decision — the sharpest
  defect found: the citation contract holds mechanically while serving another
  case's text as quotable. Cheapest detector is the opening paragraph's language
  disagreeing with the record's `language` field.
- **#50** `search_laws` does not resolve a statute reference to the article it
  names. `search_laws("Art. 60 OR")` returns articles that *cite* Art. 60 OR.
  **DONE 2026-08-22** — see #88 above.

#48 and #49 have serving-side detectors deployed; their corpus repairs need the
build window clear and an SSH session.

**#50 is narrower than the real problem.** `search_laws("Verjährung unerlaubte
Handlung")` returns Art. 133 IPRG, Art. 142 IPRG and Art. 207 AHVV — not
Art. 60 OR, which *is* the tort limitation provision. The query expands to
`(verjährung OR prescription OR prescrizione) AND unerlaubte AND handlung` and
Art. 60 OR loses because it does not repeat the query's words. So search_laws
ranks articles that *mention* a topic over the article that *governs* it, with
no explicit reference in the query at all. The #50 fix does not touch this;
it needs concept-level ranking (headings, marginal notes, or the citation
graph as a prior). Unfiled.

## Unfiled findings worth tasks

1. **`/api/decisions/<court-prefix>` 500s** — ~77 error lines and 7 ASGI
   tracebacks per hour, identical before and after the deploy, on truncated
   paths (`bger`, `zh`, `vd`, `bge`, `ch`). Pre-existing, unreported.
2. **`search_stack/reference_extraction.py`** ordinal list stops at `sexies`, so
   "Art. 322decies StGB" extracts as **Art. 322** — a wrong edge in the citation
   graph, not a missing one. Feeds `build_reference_graph.py` (publish step 2c),
   so pipeline-gated. Sits under Paper 2's substrate.
3. **`_LEGAL_STOPWORDS` is exact-match** — `Bundesgerichts` ≠ `bundesgericht`,
   `Erwaegung` ≠ `Erwägung`. Gates two surfaces now. Two tests document it.
4. **#82 residual** — a `;` between two citations in one parenthesis can still
   end a claim span.

## Maintenance pass, 2026-08-22 afternoon (five approved items)

1. **logrotate for publish.log** — installed at `/etc/logrotate.d/opencaselaw-publish`
   (repo copy `deploy/logrotate/opencaselaw-publish`, following the ocl-tier3
   pattern). copytruncate, weekly + maxsize 200M, rotate 12. The 1.1GB
   first rotation fires at the next daily logrotate run.
2. **`swiss_caselaw.db` (0 bytes, May 21)** — deleted. Zero code references.
3. **OnlineKommentar** — verified CURRENT, not stale: served DB (1,170 rows =
   390 commentaries × ~3 languages, max date 2026-07-29) matches the live API
   exactly (total=390, newest 2026-07-29). The monthly timer works — Aug 2
   mtime is its last firing. Bumped `opencaselaw-commentaries.timer` monthly →
   **weekly** (Mon 13:00 UTC; corpus is small, resync is minutes). Deleted the
   stale 51MB March copy on the volume — same trap class as the stale
   `/root/.swiss-caselaw/statutes.db`.
4. **`_incremental` twins are LIVE, do not delete.** Written Mon–Sat 20:00 UTC
   by `opencaselaw-publish-incremental.service` → `scripts/incremental_nightly.py`
   in `--shadow` mode, validated nightly by `publish_drift_check.py`. They are
   the staging ground for replacing the 7h+ full rebuild. Open decision for
   Jonas: shadow was meant to flip to `--in-place` after one green week —
   that was 2026-05-18, three months ago. Flip or keep paying ~50GB.
5. **paragraph_embeddings top-up** — armed on the VPS: `/root/embeddings_topup.sh`
   (running, gate-waiting), log `/var/log/opencaselaw-embeddings-topup.log`.
   Waits for tonight's full publish + 20:00 incremental to exit (≥21:00 UTC),
   copies the live 18.8GB DB (immutable-held by workers — never write in
   place), encodes the ~48k-decision delta with `--restart` (the offset
   watermark is invalid across structure rebuilds; the existing-key filter
   dedups), hard-stops 03:15 UTC, atomically renames on success. Workers pick
   it up per-call, no restart. Resume = rerun the script. **Verify tomorrow:**
   `tail /var/log/opencaselaw-embeddings-topup.log` should end in SUCCESS;
   then `find_relevant_erwaegung` on any 2026 decision should show the
   semantic-rescue path active.

## Five-gap exploration (evening pass) — evidence + next moves

**Jonas's picks for the next session: (1) shadow-drift diagnosis, (2) gate
timeout≠regression fix.** Homepage sync script committed; its publish.py
wiring NOT yet applied (offered, deferred — apply it alongside the gate fix
since both edit publish.py under one review).

**1. Corpus integrity.** The dedup map ALREADY EXISTS and is fresh daily:
`representation_manifest.db` — 279,359 rows, 138,447 canonical groups,
140,912 duplicates, evidence-tagged (`byte_identical`, confidence 1.0),
GE 153k + VD 101k rows in multi-member groups (= #90's cantonal duplicates),
CH only 5.9k (mostly ch_vb — the BGE dual-id problem is NOT in the manifest;
that merge is its own work item). The missing piece is CONSUMPTION: serving
never collapses members or redirects to canonical. First shippable: search
collapses multi-member groups + `get_decision` notes the canonical id.
Ready-to-run post-window sizing for #48 (seconds, indexed):
```sql
-- padded dates:      SELECT COUNT(*) FROM decisions WHERE court='bge' AND decision_date LIKE '%-01-01';
-- volume-year sweep: SELECT decision_id, decision_date FROM decisions WHERE court='bge';
--                    (pipe through _bge_volume_year_mismatch)
```
#49's corpus repair (full_text page-bleed) still needs the language-mismatch
scan over first paragraphs — post-window, decisions.db.

**#48 SIZED (2026-08-22 18:15, post-window):** 50,481 `court='bge'` records.
Padded `-01-01` dates: **5,549 (11.0%)**. Volume-year mismatch >±1y:
**3,638 of 35,305 checkable (10.3%)**. One systematic mode visible: early
volumes (1875–1876) carrying `2020-03-15` — an ingest-batch timestamp in
`decision_date`, so a `GROUP BY decision_date HAVING COUNT(*)` huge-cluster
query will likely isolate whole repairable batches at once. Caveat: 15,176
ids (30%) didn't parse under `^bge_(?:BGE_)?(\d+)_` — the true mismatch
count is a floor; inspect those id shapes before the repair pass.

**2. Shadow flip: NOT flippable — drift is real, 12/12 nights red.**
`citation_targets` −1,521,411 (−13.2%), `decision_citations` −130,809
(−1.33%), structure pair lost 3,995 decisions' paragraphs (0.51%).
decisions/statutes pairs match. The incremental builder has per-row
input-hashing + an extractor_version gate on its base DB — but the version
is bumped MANUALLY ("if this list ever grows, bump"), so extraction-code
improvements that forgot the bump leave stale rows in the shadow forever.
Diagnosis path: (a) compare `meta.extractor_version` live vs shadow,
(b) diff one drifted decision's targets live-vs-shadow, (c) if staleness
confirmed → bump version, reseed shadow from tonight's full graph, watch
drift for a week, then flip. The −13.2% concentrated in *unresolved*
targets (resolved edges only −1.33%) suggests the full builder records
unresolved extractions the incremental path drops.

**3. QC gate: both failures were TIMEOUTS, not regressions.** 08-21:
started 11:09:20, killed at exactly 12:09:20 (3600s), no verdict file.
08-18 same (during the ad-hoc-scan incident). Today: 13 min (~781s) —
the variance is page-cache temperature, and the memory's 3,518s p90 shows
the O(corpus) scan creeping to its ceiling. Structural insight: the gate
runs AFTER the swap — it gates DISTRIBUTION (HF upload, pushes), not
serving. So a timeout cascade costs distribution while protecting nothing.
**SHIPPED 2026-08-22 evening (`67698d11`), first live run 08-23 03:30:**
  a. Timeout ≠ CRITICAL — on timeout/stall the gate returns a truthy
     "WARN (…)" outcome: no cascade, distribution proceeds, ntfy DEGRADED
     alert, recorded in last_publish_success.non_fatal_failures. Exit 1
     (a real verdict, or a crash) still returns False and still blocks.
     run_cmd gained `outcome_sink` to expose HOW a command failed.
     The 3600s ceiling deliberately NOT raised — it keeps signalling that
     the scan is outgrowing its budget.
  b. The 5c-after-Step-2 reorder was considered and DROPPED: 5c already
     sits early-tier with only light steps (5a/5b/5g) between the FTS5
     build and it, so the reorder buys minutes of cache warmth at real
     review risk. The variance driver is corpus-scale + cache pressure,
     not ordering.
  c. Medium-term unchanged: incremental gate (scan rows changed since
     last green) — worth doing regardless of the cutover.
  Homepage wiring shipped in the same commit: step_6_git_push runs
  scripts/sync_homepage_fallbacks.py behind a cannot-fail helper and
  pushes docs/index.html at both push points. The proposal doc is done.

**4. Semantic rescue: bench exists and is purpose-built.**
`benchmarks/pinpoint_regeste_bench.py` — Regeste E-hints as ground truth,
reports hit/no-match "by source (lexical vs semantic)", designed to run on
the VPS. Eval plan (next session, after tonight's top-up SUCCESS):
```
PINPOINT_SEMANTIC_ENABLED=false python3 -m benchmarks.pinpoint_regeste_bench --limit 500 --court bge
PINPOINT_SEMANTIC_ENABLED=true  python3 -m benchmarks.pinpoint_regeste_bench --limit 500 --court bge
```
Compare hit/no-match; #85's repro A/B are the qualitative check. RAM for
enabling in workers: model ~500MB × 8 ≈ +4GB (host has ~53GB free) plus
first-call latency per worker; warmup call in the unit would mask it.

**5. Homepage regeneration: BUILT + tested, wiring pending approval.**
`scripts/sync_homepage_fallbacks.py` (+7 tests) rewrites the six hydration
ids from stats.json; strict (missing id / implausible value → exit 1,
nothing written); ran on the real homepage: 991'298→1'054'206, 109→118,
21'108→21'136, 8.9M→9.8M, delta 198→171, date 2026-06-21→2026-08-22.
The site serves from docs/ via the git push, so committing docs/index.html
IS the deploy. publish.py wiring (one call + one path, gated): in
step_6_git_push, run the script non-fatally before the diff check and add
"docs/index.html" to `paths`. Both push points (6a early, 6 final) then
carry it.

## Build / ops notes

- The push failure that looked like a permanent non-fast-forward divergence
  **self-heals**: `publish.py` stashes, `pull --rebase`, pops, pushes at
  finalization. It stopped pushing 08-20→08-22 because runs were not reaching
  that step, not because the divergence was unrecoverable.
- `opencaselaw-smoke.service` was failing on `publish_freshness`
  ("last successful publish 38h ago") — should clear now that 08-22 pushed.
- Corpus growth is flat: median ~123 decisions/day, unchanged since April.
- `duplicate_representations` frozen at 140,912 (13.4% of corpus) — one new
  duplicate in a month while the corpus grew +3,492. It never self-heals.
- Identity/normalisation is one programme across #76, #79, #90 and internal
  #40, #48, #49.

## Technique worth reusing

`docs/stats.json` has 420 commits back to 2026-02-12 and `docs/quality.json` 94
days. `git log --follow` + `git show <sha>:<path>` gives months of corpus and
QC trends with no VPS access and no corpus scan — the cheapest instrumentation
in the project.
