> Record of the pre-fix review (2026-09-05). Line numbers refer to the branch before the fixes listed in docs/agent-loop/proposals/2026-09-05-research-cli-release.md; every blocker and high below is fixed there, refuted claims are kept for the record.

# Release-readiness review: research CLI patch (branch codex/research-cli-20260905, base 8fda0c84)

Worktree: /private/tmp/claude-501/-Users-jonashertner-caselaw-repo-1/646eff68-408e-4c22-bd86-a021c6b9320b/scratchpad/research-cli-wt
Scope: uncommitted patch (CLI package, research_contracts.py, mcp_server.py wrapper + REST responses= + curated OpenAPI, docs, tests).
Method: eight read-only dimension reviews, each finding re-verified by three adversarial lenses; this report ranks what survived. Severities below are the post-verification ones. "Fix" = minimal concrete change.

Verdict: NOT ready to commit/deploy as is. Two independent blockers (CI collection error; Copilot Studio OpenAPI regression), both cheap to fix. The server-side wrapper is otherwise sound; the CLI has correctness gaps in search paging and citation identity that should be fixed before docs/research-cli.md is pushed (the nightly pipeline publishes docs/ to opencaselaw.ch).

---

## 1. Blockers (must fix before commit/deploy)

### B1. CI, release test job and `make test` go red at collection (WIRE-1 = REST-2 = PKG-1)
- File: tests/test_research_contracts.py:12 (`from openapi_spec_validator import validate as validate_openapi`, module level, no importorskip).
- Dependency declared only in pyproject.toml:63 (`dev` extra). Absent from requirements.txt, .github/workflows/ci.yml:23 (`pip install -r requirements.txt pytest pytest-asyncio openai anthropic google-genai`), .github/workflows/release.yml:140-142, and the repo `.venv` (mcp 1.26.0).
- Failure: `pytest -q` at repo root -> `ModuleNotFoundError: No module named openapi_spec_validator` -> "Interrupted: 1 error during collection" -> the WHOLE session aborts (2745 collected, 0 run), CI red, next `v*` server release tag red, `make test` red. Reproduced with the repo .venv. Passes locally only because venv312 had the package installed by hand; no installed distribution pulls it in transitively (Requires-Dist scan).
- Fix: add `openapi-spec-validator>=0.7` to the install line in ci.yml:23 and release.yml:140-142 (or a requirements-dev.txt consumed by both and by Makefile:117). Do not use importorskip: that would silently skip the spec validation in CI.

### B2. /api/openapi.copilot.json regresses: six curated actions lose their hand-typed 200 schemas and get an ambiguous 422 (REST-1)
- File: mcp_server.py:27908-27917 (`api_openapi_copilot` injects `_COPILOT_RESPONSE_SCHEMAS` only when the 200 schema has no type/$ref/oneOf/anyOf/allOf); research_contracts.py:204-205, 222-232 (`rest_responses` sets 200 model = `Success | ResearchError`, 422 = ResearchHTTPError); routes at mcp_server.py:27934, 27951, 28119, 28286, 28427, 28991, 29250 now carry `responses=_research_rest_responses(...)`.
- Failure: FastAPI emits `anyOf:[$ref SearchDecisionsResponse, $ref ResearchError]` for the 200, so the typed Copilot schemas are bypassed for /decisions, /decisions/{id}, /citations/{id}, /laws/{abbr}, /cite, /erwaegung/{id}/{e}; 422 becomes ResearchHTTPError whose required `detail` is `anyOf:[string, array, object]` (the exact "required property with ambiguous schema" construct the ValidationError.loc hand-fix in _custom_openapi exists to remove); 13 components added; document grows 53,440 -> 63,965 B. Any Copilot Studio / Power Automate user (Lalive) re-importing the spec hits either the 04-24 import-failure class or the 2026-05-11 "answers OK but null hits" class (docs/reports/2026-05-11-copilot-studio-fix.txt). benchmarks/copilot_openapi_validation.py: `get_schema()` returns 0 declared properties for /decisions and `validates()` raises PointerToNowhere on `#/components/schemas/SearchDecisionsResponse`. runbooks/research_cli_release.md:83 expects this document sha256-unchanged.
- Verified: both apps built in-process (origin/main vs worktree) under the production stack and diffed; benchmark helpers run against both specs.
- Fix (preferred, also resolves H1/REST-4): remove `responses=` from the seven live routes; have `research_contracts.research_openapi()` inject `TypeAdapter(...).json_schema()` into its own curated copy only. Then /api/openapi.json and /api/openapi.copilot.json stay byte-identical to today except the changed /decisions and /lookup description strings (reword the runbook sha256 step accordingly, or also revert the description edits). Alternative: in api_openapi_copilot always prefer `_COPILOT_RESPONSE_SCHEMAS.get((method, path))` when present, reset the seven 422s to `$ref HTTPValidationError`, re-trim components. Either way add an offline test asserting the copilot 200 schemas for the 15 allow-listed paths equal `_COPILOT_RESPONSE_SCHEMAS`.

---

## 2. High (fix before deploy)

### H1. Full /api/openapi.json: 422/404 override to ResearchHTTPError (ambiguous required `detail`, MCP-only `ignored_arguments` in HTTP error bodies) (REST-4)
- File: research_contracts.py:29-31 (ResearchHTTPError inherits ResearchModel), :222-233 (rest_responses attaches it to 422 on all seven routes, 404 on get_decision/get_law). `_sanitize_for_3_0` (mcp_server.py:27095-27159) only flattens null-branch anyOf, so the construct survives in all three documents.
- Failure: integrator imports /api/openapi.json into Copilot Studio / Power Automate (docs advise either spec) -> "Required property detail cannot have an ambiguous schema", or codegen emits a union where today it is `list[ValidationError]`.
- Fix: covered by the preferred B2 fix (no `responses=` on live routes). If responses= stays: do not override 422/404 (keep HTTPValidationError); if a string-detail variant must be documented, define it without the ResearchModel base and `detail: str` only.

### H2. `ocl decisions search` concatenates non-composable relevance pages: duplicates and lost rows, counted as complete, exit 0 (PAG-1)
- File: clients/python/src/opencaselaw_cli/cli.py:171-191 (loop, `results.extend(rows)` at :184, no decision_id dedup), :197-201 (returned=len(results), max_results_reached, exit 0). Server: mcp_server.py:7571-7578 (`_target_candidate_pool` = max(60, 4*(offset+limit))), :7020 (slice), :4113-4132 (page-local interleave).
- Failure (measured live): `ocl decisions search Rachekündigung --max-results 25 --page-size 5` -> 25 rows / 22 unique (3 duplicates), 4 decisions present in a single limit=25 request (incl. bge_BGE_132_III_115) absent; output says returned:25, no error. Defaults (--max-results 50 --page-size 50) are a single request and unaffected. Server behaviour is pre-existing; the CLI's defect is composing and counting such pages.
- Fix: for a non-empty query and max_results <= 800 issue ONE request with limit=max_results (server compact cap 800 = CLI page_size cap); keep the loop only for filter-only enumeration (stable ORDER BY, exact totals) and for max_results > 800. Wherever paging remains: dedupe by decision_id, report `duplicates_dropped` and a `ranked_pages_not_composable` flag in `_client`, do not count duplicates toward max_results. Regression test with an overlapping second fixture page (clients/python/tests/test_cli.py:22-26 `page()` currently models perfectly stable pages). Document in docs/research-cli.md:144-146 (PAG-3).

### H3. `citations resolve` never reaches status=resolved for docket references (CIT-1)
- File: clients/python/src/opencaselaw_cli/workflows.py:316 (limit=25 /api/lookup), :329, :333-335 (len(candidates) >= 25 -> resolution_incomplete). Server: mcp_server.py:24554-24594 (`_lookup_case_number` total=len(out), delegates to search_fts5 which pads the page with +/-40-serial docket-family rows / BM25 hits).
- Failure (reproduced by running `_resolve_one` against saved live responses): `echo 4A_747/2012 | ocl citations resolve --stdin` -> resolution_incomplete, "candidate window is capped", exit 4, although /api/cite returned exists=true, decision_id=bger_4A_747_2012 and exactly one lookup row label-matches. Same for 4A_247/2020. The documented docket workflow in docs/research-cli.md ("Resolve a citation list") therefore never resolves a BGer docket.
- Fix: treat the window as capping only when the LABEL-MATCHING rows reach 25, not when the page is full; use the resolved decision's own `docket_number` (server exact match) as identity evidence: resolved iff `_reference_key(decision['docket_number']) == key` (after M2's separator normalisation) and no second label-matching decision_id in the lookup. Do not rely on candidate ordering (FTS fallback path is BM25-ordered). Land together with M2 and M3.

### H4. `decisions get/passage` and `citations list` accept any exists=true from /api/cite: fuzzy server match used silently (CIT-4)
- File: clients/python/src/opencaselaw_cli/cli.py:257-270 (`_decision_id`: any reference containing '/' -> /api/cite, returned decision_id used if exists is True and has no '/'); used at :280, :313, :325. Server ladder mcp_server.py:6218-6296 (exact docket -> newest by date, then `LIKE '%x%'` for any docket-like fragment; `_input_is_docket_like` :6136-6148 accepts '247/2020').
- Failure: `ocl decisions passage 247/2020 2` -> LIKE scan, newest of 4A_/6B_/2C_/1C_247/2020 -> verbatim Erwägung of an arbitrary chamber's decision printed as the requested one, exit 0, no warning; `citations list 247/2020` lists that decision's graph. Unlike `citations resolve` there is no label/ambiguity guard. Code-read only (LIKE branch deliberately not exercised live during the build window). test_cli.py:193-204 covers only the exact-docket path.
- Fix: after /api/cite, fetch /api/decisions/{id}?full_text=false and require the same identity evidence as `citations resolve` (docket_number / citation_string label match after M2/M3 normalisation); otherwise fail with exit 4 "not an exact docket; use citations resolve or a canonical decision_id". Or restrict these commands to canonical ids and exact server citation strings.

### H5. Guide examples use a pinpoint that does not exist in the corpus (DOC-1)
- File: docs/research-cli.md:58, :61, :122 (bge_BGE_140_III_86 with E. 2.3; propagated from README.md:166). Live corpus has only E. 2, 4.1, 4.2 for that decision.
- Failure: `ocl decisions passage bge_BGE_140_III_86 2.3` -> error + available_e_numbers, exit 3; the `citations resolve` example -> pinpoint_unavailable, partial, exit 4; `ocl cite 'BGE 140 III 86' --pinpoint 2.3` -> prints "BGE 140 III 86, E. 2.3" exit 0, i.e. the guide mints a citation to a non-existent Erwägung directly under its own warning against that. docs/ is published to opencaselaw.ch by the nightly pipeline.
- Fix: use `4.1` (or a decision with a richer E-structure) in all three lines and in README.md:166; state that `cite --pinpoint` formats without verifying passage existence (no `pinpoint_exists` on the wire; see L-CIT-6).

---

## 3. Medium / follow-ups

### Medium

- M1. `bundle create --max-results > 50` silently loses selection rows (PAG-2). workflows.py:110 hard-codes limit=min(50, remaining) and walks next_offset; dedup at :125-126 hides duplicates but rows in one pool's top-N and the other's tail are never seen; manifest says complete, exit 0; --resume reproduces the gap. Fix: one request with limit=min(max_results, 800) for a non-empty query; page only beyond 800 or for filter-only requests; record non-composability in `completeness`; fixture test with overlapping pages.
- M2. `_reference_key` does not fold the federal docket separator (CIT-2). workflows.py:274-282: '4A_247/2020' -> '4a_247/2020' vs stored '4A 247/2020' -> '4a247/2020' (66,456 federal rows stored in space form, mcp_server.py:11726-11745). Masked today by H3; after H3 these flip to `unrecognized`. Fix: comparison-only normalisation of `\d{1,2}[A-Za-z]{1,3}[._ ]\d{1,6}[/_]\d{4}` to PREFIX_serial/year on both sides (mirror docket_aliases._KEY_RE); never use the key for output (R1).
- M3. Embedded-pinpoint and bare-tuple BGE references reported unrecognized/ambiguous although the server resolved them exactly (CIT-3). workflows.py:304-338: 'BGE 141 III 433 E. 2.3' -> 'bge141iii433e.2.3' never equals 'bge141iii433'; '141 III 433' -> ambiguous; even the server's own output 'BGE 141 III 433, E. 2.3' does not round-trip; dual-id BGEs (legacy `bge_147 III 586` + `bge_BGE_147_III_586`) yield "disagreement between resolvers" for one decision. Reproduced with saved live responses. Fix: for the comparison key fold a leading bge/atf/dtf and strip a trailing `, E./Erw./consid. n` as `_bge_ref_candidates` does; compare against citation strings AND decision['docket_number']; treat two label-matching ids as one decision only when their server-supplied citation_string_de is identical.
- M4. Untracked `.venv` symlink in the worktree would be staged by `git add -A` (T2). `.gitignore:50` is `.venv/` (directory pattern, does not match a symlink); `git add -n .venv` -> `add '.venv'`. Fix: remove the symlink before committing and stage files explicitly (never `git add -A`); optionally add `.venv` without slash to .gitignore.
- M5. isError=true for handled not-found is a new contract, inconsistent across the six tools and with `_classify_outcome` (WIRE-3). mcp_server.py:25045-25048 `isError=bool(payload.get("error"))`: get_decision unknown id, get_erwaegung "E. not found", get_law "No law found", find_citations graph-missing -> isError=true; `cite` unresolved -> isError=false; `_classify_outcome` (:2149-2157) labels the same call `empty`. LangChain adapters raise ToolException on isError; integrators keying retries on isError retry deterministic misses. Design decision, not a bug: pick one rule (recommended: isError only for input errors, backend failures, timeouts, contract violations; not-found = isError=false + ResearchError payload) and document it in docs/research-cli.md and tool descriptions; keep structuredContent on error results.

### Low (document or fix opportunistically)

- L-WIRE-2. Wire payload doubles: get_decision text and structuredContent both carry full_text capped at 200k (mcp_server.py:25517-25527, 25591-25599): 189 KB -> 378 KB; search fields=full x2.56. Fix: omit full_text from structuredContent (keep full_text_url/full_text_total_chars) or use a lower structured cap; document the size budget.
- L-WIRE-5. Contract-violation path logs only the tool name and tells the client "please retry" for a deterministic failure (mcp_server.py:25034-25043). Fix: catch pydantic.ValidationError, log `[(e['loc'], e['type']) for e in exc.errors(include_input=False)][:5]`; change the text to "did not match the published schema; use the REST endpoint".
- L-BI-1. Passage .txt artifacts carry server auto-link Markdown (`_auto_link_citations`, mcp_server.py:12344) while docs/research-cli.md:92-93 and the manifest evidence_contract say "unmodified". Decision full_text is unaffected. Fix: say so in evidence_contract/docs, or add a server `text_raw`/`?links=false` and store that; never strip client-side.
- L-BI-2. `--law` cannot reach cantonal statutes: `ZH/StG` -> %2F -> 404; `--canton` is a search filter only, so `--law StG:1 --canton ZH` saves the federal Stempelabgabengesetz as "saved" (workflows.py:253-256; mcp_server.py:19451-19456). Same %2F issue in cli.py:317. Fix: parse `CANTON/ABBR:ART` and send `canton=` as a query param; document that search `--canton` does not apply to statutes.
- L-BI-3. Bundle `--language en|rm` is forwarded to /api/laws (workflows.py:256) -> HTTP 500 on every --law item, repeated on --resume (cli.py:47-49 has no choices restriction; mcp_server.py:19546-19549). Fix: `--law-language {de,fr,it}` or map non-de/fr/it to de and record it in request.laws.
- L-CIT-5. BGE pinpoint-page ('BGE 129 I 236') and trailing-punctuation references become status=missing; the server's `queried_page_within_this_decision` close_match hint is dropped, and under --fields the citation object is gone (workflows.py:293-295; cli.py:205-221). Fix: distinct status (e.g. page_within_decision) when that match_reason is present; lift close_matches (server strings only) to the row and retain it in _project; never auto-resolve.
- L-REST-3. LookupResponse.results typed as list[DecisionRecord] but /lookup hits carry `citation`/`url`, never citation_string_*/canonical_url (research_contracts.py:174-179; mcp_server.py:24581-24591). Fix: LookupHit model (decision_id, docket_number, court, canton, decision_date, title, citation, url), extra='allow'.
- L-TR-1. http.client.HTTPException (IncompleteRead/BadStatusLine/LineTooLong) escapes Client.get as a traceback, exit 1 (client.py:108 catches URLError/TimeoutError/OSError only; cli.py:340-356). Fix: add http.client.HTTPException to the retryable clause; test with a read() raising IncompleteRead.
- L-TR-2. UTF-8 BOM on --input/--stdin makes the first line a bogus identifier (cli.py:131 'utf-8', :134 strip does not remove U+FEFF). Fix: 'utf-8-sig' and lstrip('﻿'); test with a BOM fixture.
- L-CIT-6. `cite --pinpoint` guard at cli.py:291/:211 checks `pinpoint_exists`, a field the server never emits (grep = 0 in mcp_server.py); `ocl cite 'BGE 141 III 433' --pinpoint 99` exits 0 with "E. 99". Fix: drop the dead check and document, or call /api/erwaegung when --pinpoint is given and exit 4 on miss.
- L-CIT-7. `citations resolve` does not lift `composed_of` for parent-level pinpoints (workflows.py:340-347 vs :204-206); invisible under --fields. Fix: copy composed_of to the row and retain it.
- L-PAG-3. docs/research-cli.md:144-146 implies paged relevance results compose; docs/api/index.html:155 (and DE/FR/IT/RM copies) says to advance by `returned`, which is wrong on the MCP path (`_consumed` vs post-dedup `returned`, mcp_server.py:25277, 25394-25398; WIRE-9). Fix: "follow next_offset only"; note single-request for ranked queries.
- L-BI-4..11 (bundle): fallback-language/alias articles saved as normal with provenance.language = requested (BI-4); no fsync/backup of manifest (BI-5); no writer lock (BI-6); one deleted artifact makes the bundle unresumable with no repair path (BI-7); retry overwrites the item entry, orphaning the earlier artifact pointer (BI-8); foreign manifests crash with KeyError instead of exit 2 (BI-9); NaN/lone-surrogate payloads abort without recording the item (BI-10); provenance omits docket_number and statute notes (BI-11). All read-code, small fixes, none affect evidence integrity of what IS saved.
- L-WIRE-6. `_prepend_arg_warning` NOTE lands inside the `error` string when the inner handler returns a plain TextContent list (mcp_server.py:24746-24765, 25024-25031). Fix: strip the note before json.loads in the non-tuple branch.
- L-WIRE-7. `erwaegungen_paragraph.text` nullable in DDL; a NULL row would become response_contract_error (see open question Q2).
- L-WIRE-8. tools/list grows 55.9 -> 81.2 KB (+45%) from verbose $defs/title/default:null. Optional post-processing of output_schema().
- L-REST-5. Shared 404 text "Requested decision or XML representation unavailable." on /laws/{abbreviation} (research_contracts.py:231); unknown laws return 200. Per-operation text.
- L-PKG-3/4/5. Four hard-coded '0.1.0' literals (pyproject:7, __init__:6, cli.py:62, client.py:80); wheel METADATA lacks urls/authors/classifiers; `ocl` console-script name may collide (unverified). Optional.
- L-DOC-2/3/4/5. API page omits GET /api/lookup (docs/api/index.html:165-219); homepage access-paths not mirrored to src/pages/index.html (build_docs --check drift, no-clobber guard protects the live page); README:214 "Research REST API" row links to the HuggingFace download section; /api/mock-decision listed as GET but is POST (pre-existing).
- L-TR-3/4/5/6/7. No circuit breaker in batch loops (1,000 IDs x 3 attempts during an outage); Windows locale encoding on stdout (`sys.stdout.reconfigure('utf-8')`); TLS verification failures and read timeouts retried like transient errors (fail fast on SSLCertVerificationError; default 30 s < nginx 60 s); explicit `"pinpoint": null` in a JSONL row discards --pinpoint (cli.py:283 `row.get('pinpoint', args.pinpoint)` -> use `or`); unbounded body/redirect hardening (only with a hostile --base-url).
- L-T3/T4/T5/T6 (tests): parity tests mock all six handlers so real backend shapes never pass strict validate_payload offline (add one fixture-DB test per tool through the real handler asserting not isError); tests/test_decision_widget.py:3-6 docstring still documents the removed list-shape invariant; wire-level gaps (condensed_terms, alias normalisation, graph-missing find_citations, SDK input-validation path answering without structuredContent); no test that `_now_hhmm(None)` reads the wall clock.

---

## 4. Refuted claims

- WIRE-4 (cantonal NULL article_num -> response_contract_error): mechanism real (LawArticle.article_num: str strict; wrapper replaces payload), but no producer in the pipeline can emit a None article_num (lexfind_cantonal.py regex group, zh/ti/sil scrapers all str); the nullable DDL and `or ""` guards are defensive, not evidence of NULL rows. Latent hardening item only (see Q1).
- PKG-2 (no CLI release path; `v*` tags route to the server package): mechanical facts hold, but the patch ships runbooks/research_cli_release.md whose section 7 states the CLI is source-install only, not tagged/published; nothing running today is affected. Process note, not a defect.
- Lead observation (3) (`_prepend_arg_warning` mutates payload in place): mutation exists but every mutated payload is per-call (deep-copied row/results, fresh dicts, json.loads from SQLite cache); no shared-object corruption. Refuted as a defect.
- Lead observation (4) (strict validation turns real calls into contract errors): strict mode does reject int-for-bool/str and missing keys, but every reachable real shape validates (all-TEXT decisions.db columns, bool()/int() coercions in handlers, statutes.db NOT NULL article_num/text, 8 live REST payloads incl. cantonal TOC, close_matches, composed structure, ECtHR). No production path found that fails; the risk is future drift (L-T3).
- Lead observation (5) (CLI next_offset assertion vs MCP `_consumed`): the CLI targets REST, where next_offset == offset + returned always holds (no dedup, compact truncation covered by the 800 cap); the MCP divergence is real but does not affect the CLI (documented as L-PAG-3/WIRE-9).
- Lead observation (7): six new i18n keys per language, not five; parity test passes.

## 5. Verified as fine

- SDK behaviour (mcp 1.27.0 read at mcp/server/lowlevel/server.py:540-541): a returned CallToolResult passes through unchanged; pydantic strict is the only server-side gate; the four legacy tuple-returning tools keep SDK jsonschema validation. Wrapper guards empty content, non-JSON text, non-dict payloads; timeouts, handled errors, schema violations and unknown-argument warnings all yield structuredContent (nothing declares outputSchema and answers without it, except the SDK's own input-validation path, pre-existing). Commercial open-access note applied exactly once on both paths.
- No import cycle; `_enrich_with_citation` has exactly one module-level definition; research_contracts imports only pydantic + stdlib; production import closure works from /opt/caselaw/repo (sys.path[0] = repo dir); tests/test_packaging_runtime_closure.py passes; root wheel ships research_contracts.py and not clients/; release gate `len(_list_tools()) == 44` holds under mcp 1.27.0 and 1.29.1.
- R1-R3 on the client: no citation construction (`_reference_key` is comparison-only; outputs carry server citation_string* verbatim); quotations only from /api/erwaegung with decision_id/e_number/non-empty-text validation; close_matches never adopted; decision round-trip guard requires decision_id equality; no false identity reaches status=resolved for any input tried.
- Bundle storage: hashes cover exactly the bytes written; --resume re-hashes before any network call; artifacts opened 'xb'; manifest replaced via temp file + os.replace; symlink/traversal/absolute/.. paths rejected (reproduced); crash-between-artifact-and-checkpoint leaves an unlisted orphan and resume writes a fresh one (tested); saved items skipped with zero calls; failed page leaves next_offset unchanged; completeness reporting honest (partial + exit 4 on any non-saved item or search error); decision .txt artifacts are byte-exact full_text (REST path has no cap/auto-link).
- REST pagination unchanged by the patch (has_more/next_offset/page_cap byte-identical to origin/main); `copy.deepcopy` on /decisions and /decisions/{id} is a correctness improvement (previous in-place mutation of cache-owned dicts); `responses=` is documentation-only (contract-violating bodies still returned verbatim, verified in-process); full spec keeps route list, operationIds, parameters, tags, openapi 3.0.3; all five generated documents validate with openapi-spec-validator 0.9.0; /api/research/openapi.json has 13 components, 0 dangling refs, paths == RESEARCH_PATHS, is proxied by nginx `location /api/` with the normal zone, include_in_schema=False.
- Transport: retry/backoff matrix verified by execution (429/502/503/504 + network only; Retry-After honoured to 30 s, garbage -> exponential, >30 s -> fail fast; exactly retries+1 attempts); 200 ms pacing = 5 r/s under nginx 30 r/s burst 50; base-URL validation rejects credentials/path/query/fragment/non-http; path params quoted safe=''; no path traversal or route confusion reachable; only Accept and User-Agent sent; exit codes 0/2/3/4/130 match the docs; BrokenPipe handled; JSONL grammar validated before the first request (except BOM).
- Docs: every documented command/flag exists in build_parser(); exit-code table, retry numbers, rate guidance, "not on PyPI" statements, seven-route/six-tool claims, anchors (#cli, #research-api-contract), i18n keys in all five languages, nightly sync anchors (ten ids exactly once), HTML tag balance all check out; the CLI works against the live (old-code) API for every route it calls; `--law OR:336` passes the bundle law check live.
- Tests: 2736 passed / 24 skipped in 57 s under venv312 (mcp 1.27.0); 151 targeted tests pass under both 1.27.0 and 1.29.1; client tests offline and deterministic (injected transport/sleep/clock); `--now-utc 20:00` in test_incremental_per_pair_cutover.py is justified by the dedicated late-start tests and fixes the after-22:30-UTC CI failure at the base; anti-spread outputSchema guard preserved as an exact 10-tool set; widget-specific guard preserved.
- CLI is stdlib-only and parses as 3.10 syntax (ast feature_version); no 3.11+ APIs; not executed under 3.10 (no interpreter).

## 6. Open questions for the lead (live checks)

- Q1. WIRE-4 closure: after the build window (or on a copy), run `SELECT COUNT(*) FROM articles WHERE article_num IS NULL OR article_num=''` on cantonal_laws.db. If 0, the refutation is final; if >0, coerce in `_get_law_cantonal` or relax `LawArticle.article_num` to `str | None` before deploy.
- Q2. WIRE-7: `SELECT COUNT(*) FROM erwaegungen_paragraph WHERE text IS NULL` on the VPS decision_structure.db (local copy is 0 bytes). >0 means get_erwaegung can emit response_contract_error for real paragraphs.
- Q3. Does anyone (Lalive) re-import /api/openapi.copilot.json on a schedule, or is the connector frozen from the 05-11 import? Determines whether B2 is a deploy-day incident or a latent one; either way the runbook sha256 step must be true after the fix.
- Q4. Search page determinism: the offset-20 page differed from slice [20:25] of a limit=25 request even with identical pool size and language=de, and an identical offset=15 request returned different rows on repeat (soft-deadline strategy skipping, mcp_server.py:3729-3733). Worth a controlled off-peak check to decide whether ranked results need a `degraded`/`deadline_hit` flag in meta (PAG-4).
- Q5. isError policy (M5): decide (a) not-found = isError=false, or (b) keep isError=true and document; check how the Claude/ChatGPT connector hosts render isError results before choosing.
- Q6. TS-SDK/Ajv handling of `$defs` + anyOf root and of structuredContent on isError results was stated from recall, not fetched source; one check against @modelcontextprotocol/sdk client.ts `callTool` validation path would close it.
- Q7. Corpus-level duplicate decision_ids (VD, per project memory) mean a search row and the fetched /api/decisions/{id} can be different decisions sharing an id; the client cannot detect it. Confirm the 09-04 VD dedupe has landed before the CLI's evidence contract claims per-id identity.
- Q8. Does the `ocl` console script collide with an existing PyPI distribution's script (unverified; consider a second entry point `opencaselaw` and documenting `python -m opencaselaw_cli`).
- Q9. Execute the client test suite once under a real Python 3.10 (Requires-Python >=3.10) before any release; only static checks were possible here.
