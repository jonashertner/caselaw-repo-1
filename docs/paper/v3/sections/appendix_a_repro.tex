All claims in this paper are backed by code, data, and configuration
that anyone can clone or query. This appendix lists every claim's
provenance so a reviewer can verify in less than five minutes.

\paragraph{Code.}
\begin{itemize}
\item \textbf{Repository}: \url{https://github.com/jonashertner/caselaw-repo-1}
  (MIT licence). The release tag for this paper is
  \texttt{paper-resource-2026-05} (to be assigned at submission;
  prior to that, all numerical claims trace to the commit pinned in
  the HuggingFace dataset's \texttt{manifest.json}).
\item \textbf{Five-rail audit pipeline}:
  \path{mcp_server.py}, function \texttt{\_handle\_attest\_response}.
  The four
  deterministic rail implementations are inline in the same file:
  \texttt{\_audit\_statutes} (R2),
  \texttt{\_audit\_quotes} (R3),
  \texttt{\_audit\_dates} (R4),
  \texttt{\_audit\_grounding} (R5). R1 (case-citation existence) is
  inline in \texttt{\_handle\_attest\_response}.
\item \textbf{Quality framework}: \texttt{quality/} package
  (\texttt{runner.py}, \texttt{cli.py}, \texttt{drift.py},
  \texttt{baseline.py}, \texttt{smoke.py}, plus 17 modules
  in \texttt{quality/checks/} containing 63 \texttt{check\_*}
  functions).
\item \textbf{Cross-lingual harness}:
  \texttt{benchmarks/run\_cross\_lingual.py}.
\item \textbf{Pipeline guards}:
  \texttt{build\_fts5.py} \texttt{\_normalize\_*} /
  \texttt{\_dedup\_*} / \texttt{\_truncate\_*} passes.
\end{itemize}

\paragraph{Data artifacts.}
\begin{itemize}
\item \textbf{Corpus}: HuggingFace \texttt{voilaj/swiss-caselaw}
  (CC0). Each release is accompanied by a
  \texttt{manifest.json} listing per-court row counts plus SHA-256.
\item \textbf{Cross-lingual benchmark v1}: HuggingFace
  \path{voilaj/swiss-legal-rag-bench/cross_lingual_v1.jsonl}
  (CC0). 150 queries, anchored on the citation graph.
\item \textbf{Audit-bench v2}: same repository,
  \texttt{verification\_seed\_v2.jsonl}.
\item \textbf{Citation graph}: derived from the corpus via
  \path{search_stack/build_reference_graph.py}; release snapshot
  available on Zenodo (DOI to be assigned at submission).
\item \textbf{Statute mirrors}: \path{statutes.db}
  (federal, derived from Fedlex SPARQL endpoint),
  \path{cantonal_laws.db}
  (cantonal, derived from LexWork+SIL+ZH portals via
	  \path{scrapers/cantonal_laws/}).
\end{itemize}

\paragraph{Live system (runtime verification).}
\begin{itemize}
\item \texttt{https://mcp.opencaselaw.ch/sse} (MCP server endpoint).
\item \texttt{https://opencaselaw.ch/} (corpus dashboard, multilingual).
\item \texttt{https://opencaselaw.ch/quality.html} (live dataset health).
\item \texttt{https://opencaselaw.ch/coverage/} (per-source scrape health).
\end{itemize}

\paragraph{Reproducing key numerical claims.} A reviewer can verify
the paper's three headline claims in under a second from a fresh
clone, without rebuilding the corpus, via the \texttt{Makefile}'s
\texttt{verify} target:
\begin{verbatim}
git clone https://github.com/jonashertner/caselaw-repo-1
cd caselaw-repo-1
make verify
\end{verbatim}
This calls \texttt{/health} on the live MCP server (corpus size),
checks the citation-graph resolution rate against the JSON snapshot
	under \path{docs/paper/v3/tables/}, and validates the cross-lingual
benchmark MRR/Hit@10 against the committed results JSON under
	\path{benchmarks/swiss_legal_rag_bench/results/}. The expected
output is ``All headline claims reproduce''.

For deeper verification (full citation-graph rebuild and benchmark
re-runs against the live corpus):
\begin{verbatim}
git checkout paper-resource-2026-05
pip install -r requirements.txt
huggingface-cli download voilaj/swiss-caselaw --local-dir output/
python -m quality.cli run                    # 4-layer dataset health
python benchmarks/run_search_benchmark.py    # Tab.~2 baseline
python benchmarks/run_cross_lingual.py       # Tab.~3, Fig.~2 (cross-lang)
	python -m benchmarks.citation_resolution_analysis
make test                                     # full pytest suite (~40 s)
\end{verbatim}

\paragraph{Telemetry, costs, latency.} The MCP server exposes
\texttt{/health} (corpus row count) and is monitored every 5 minutes
by a systemd timer that probes 6 endpoints (search, decision, export
docx/pdf/bib/ris) with $p_{95}<2$\,s. Every audit run since
2026-05-01 is logged with full per-rail latency and judge cost
to \texttt{logs/audit\_telemetry.jsonl}.

\paragraph{Hardware.} Production runs on a Hetzner CCX43 dedicated
instance (16 vCPUs, 64\,GB RAM, 200\,GB attached volume). The full
nightly publish takes 3\,h 50\,min, dominated by the FTS5 rebuild.

\paragraph{Licences.}
\begin{itemize}
\item Code: MIT.
\item Corpus packaging + benchmark: CC0 to the extent rights exist. Court decisions in Switzerland are official published acts.
\item Commentaries: CC-BY-4.0 (OnlineKommentar.ch) / CC-BY-SA-4.0
  (OpenLegalCommentary.ch).
\item Federal statutes: derived from Fedlex SPARQL endpoint; Swiss
  Confederation copyright applies, fair-use distribution permitted.
\end{itemize}
